{
  "name": "@todica/server",
  "version": "1.0.0",
  "private": true,
  "description": "Todica サーバ (Hono + better-sqlite3 + drizzle-orm).",
  "author": "BigTree777",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/BigTree777/todica.git",
    "directory": "server"
  },
  "type": "module",
  "main": "./src/app.ts",
  "scripts": {
    "test": "vitest run",
    "build": "tsc",
    "start": "cd .. && node --env-file-if-exists=.env server/dist/src/main.js",
    "dev": "cd .. && node --env-file-if-exists=.env node_modules/vite-node/dist/cli.mjs --watch server/src/main.ts"
  },
  "dependencies": {
    "@hono/node-server": "*",
    "@todica/domain": "*",
    "bcrypt": "^6.0.0",
    "hono": "*"
  }
}
