# バックログ

> 課題・要望の一覧。優先度と状態で管理する。粒度が大きいものは機能ごとに
> [`../features/`](../features/_template/spec.md) へ切り出す。

## 凡例

- 優先度: P0(必須) / P1(高) / P2(中) / P3(低)
  - P0 = v0.1.0 に含める / P1 = v0.2.0 / P2 = v0.3.0〜v0.4.0 / P3 = v0.5.0 以降
- 状態: Todo / Doing / Done / Hold
- リリース対応は [`roadmap.md`](roadmap.md) の「リリース計画」を参照.

## 一覧

| ID | タイトル | 優先度 | 状態 | メモ |
| --- | --- | --- | --- | --- |
| BL-001 | タスク CRUD（起票・名称編集・期限切替・削除） | P0 | Done | FR-001, FR-002, FR-005, FR-007, FR-009 / v0.1.0 / `features/task-crud/` 完了. サーバ API + ドメイン + Web UI（TodayView）すべて実装済み |
| BL-002 | タスク優先度（3 段階の付与・変更） | P0 | Done | FR-003, FR-004 / v0.1.0 / `features/task-priority/` 完了. サーバ PATCH priority + TodayView の優先度サイクルボタン実装済み |
| BL-003 | タスク完了アクション（ゴミ箱経由 + カウント +1） | P0 | Done | FR-006, FR-060 / v0.1.0 / `features/task-complete/` (カウント +1 は BL-008 待ち) |
| BL-004 | 2 階層固定（サブタスク・ネストを持たない構造保証） | P0 | Done | FR-008, OOS-003 / v0.1.0 / BL-001 (`features/task-crud/`) 内で完了. Task に parentTaskId フィールドを持たず, API でも受理しない (server/__tests__/integration/tasks.test.ts) |
| BL-005 | 今日ビュー（入口・優先度順表示・"次の 1 つ" の一意化） | P0 | Done | FR-010, FR-011, NFR-013 / v0.1.0 / `features/today-view/` 完了. /api/v1/today 新設, 並び順 priority → createdAt → id |
| BL-006 | 現在のタスク（フォーカス）と完了時の自動繰上げ | P0 | Done | FR-012, FR-013, NFR-011 / v0.1.0 / `features/focus-task/` 完了. GET/PUT /focus + /today に currentTaskId 同梱 + complete/delete/期限 tomorrow で自動解除 |
| BL-007 | 今日 → 明日 への期限切替（今日ビュー導線） | P0 | Done | FR-005, FR-014 / v0.1.0 / BL-001 + BL-005 + BL-006 で完了. PATCH /tasks/{id} dueDate 受理 (BL-001), 「明日へ」「今日へ」トグルボタンと today 後の refetch (BL-005), today→tomorrow で focus 自動解除 (BL-006) |
| BL-008 | 今日の完了タスク数カウントの表示 | P0 | Done | FR-040 / v0.1.0 / `features/completion-counter/` 完了. counter テーブル + GET /counter + complete で +1 + /today に completionCount 同梱 + UI 「今日の完了: N」表示 |
| BL-009 | 境界時刻の設定（ユーザー設定値） | P0 | Done | FR-041, FR-042, NFR-012 / v0.1.0 / `features/settings-day-boundary/` 完了. GET/PATCH /settings + dayBoundaryTime (HH:MM) + SettingsView |
| BL-010 | 日次リセット処理（カウント 0 クリア + 未完了繰越 + ゴミ箱清算）の冪等実行 | P0 | Done | FR-043, FR-051, FR-062, NFR-020 / v0.1.0 / `features/daily-reset/` 完了. POST /reset + GET /today 自動実行. purgeTrash スタブは BL-011 で充填 |
| BL-011 | ゴミ箱（閲覧・復元・手動「空にする」） | P0 | Done | FR-060, FR-061, FR-062 / v0.1.0 / `features/trash/` 完了. GET /trash + POST /trash/:id/restore + DELETE /trash + purgeTrash 本実装 |
| BL-012 | タスク削除アクション（ゴミ箱経由・カウント非加算） | P0 | Done | FR-007, FR-060 / v0.1.0 / BL-001 の DELETE ハンドラ（trashedReason="deleted"）と BL-008 のカウント非加算テスト（counter.test.ts "DELETE では+1しない"）で完了 |
| BL-013 | サーバ基盤（Hono + better-sqlite3 + drizzle-orm + 単一認証トークン + HTTPS） | P0 | Done | NFR-002, NFR-020, NFR-021, NFR-032 / v0.1.0 / ADR-0007, ADR-0010 / `features/server-foundation/` 完了 |
| BL-014 | Web クライアント基盤（React + Vite + React Router） | P0 | Done | NFR-010, NFR-013 / v0.1.0 / ADR-0008 / `features/web-client-foundation/` 完了. react-router-dom v6 導入, TrashView + ルーティング設定 |
| BL-015 | API 基盤（REST + Idempotency-Key + If-Match 楽観ロック + OpenAPI） | P0 | Done | NFR-020 / v0.1.0 / ADR-0010. BL-001〜BL-011 で REST + Idempotency-Key + If-Match + OpenAPI 全実装済み |
| BL-016 | プロジェクト管理（作成・名称変更・削除. アーカイブなし） | P1 | Done | FR-020, FR-021, FR-022 / v0.2.0 / `features/project-crud/` 完了. POST/GET/PATCH/DELETE /projects + ProjectsView + TodayView プロジェクト選択 |
| BL-017 | ルーティン機能（定義・編集・指定曜日の自動生成・翌日非持越・履歴なし） | P1 | Done | FR-030, FR-031, FR-033, FR-034, FR-035 / v0.2.0 / `features/routine/` 完了. POST/GET/PATCH/DELETE /routines + 日次リセット統合 + RoutinesView |
| BL-018 | PWA 化 + オフライン書込キュー（Service Worker + IndexedDB + Background Sync） | P2 | Done | NFR-031 前提 / v0.3.0 / ADR-0008 / `features/pwa-offline-queue/` 完了. vite-plugin-pwa + TanStack Query + IndexedDB 書込キュー + ConflictDialog 実装済み. PR #19 |
| BL-019 | Android ラップ（Capacitor サーバモード + Play Internal Testing 提出） | P2 | Done | NFR-030, NFR-031, RISK-002 対策 / v0.4.0 / ADR-0009 / `features/android-server-mode/` 完了. Capacitor + SetupView + SettingsView サーバ設定 + android/ 生成 + 署名設定 / auditor Pass |
| BL-020 | Android ローカルモード（@capacitor-community/sqlite + 端末内リセット処理） | P3 | Done | NFR-021, FR-043, FR-051, FR-062 / v0.5.0 / ADR-0009, ADR-0011 / `features/android-local-mode/` 完了. Local Repository 実装 + LocalResetUsecase + local-db.ts DDL + main.tsx モード分岐 / auditor Pass |
| BL-021 | PM ツール非連携の明示（FR-070 への適合確認） | P1 | Done | FR-070, OOS-010 / v0.2.0 / auditor 確認済み: API・UI・ドキュメント全観点で適合、追加実装不要 |
| BL-022 | OSS 公開準備（LICENSE・依存関係棚卸し・秘密情報取扱い・公開 README） | P3 | Done | NFR-050 / v1.0.0 / `features/oss-release-prep/` 完了. LICENSE + README + CONTRIBUTING + dependency-licenses.md + secret-scan-report.md + package.json フィールド / auditor Pass |
| BL-023 | Google Play Store 公開対応（プライバシーポリシー・データセーフティ・審査対応） | P3 | Done | NFR-030, CONSTRAINT-003 / v1.0.0 / `features/play-store-release/` 完了. privacy-policy.md + store-listing.md + data-safety.md + policy-checklist.md / auditor Pass |
| BL-024 | v1.0.0 安定化（1 か月以上のドッグフーディング + 要件カバレッジ最終監査） | P3 | Done | SC-001, FR-001〜FR-070 / NFR-001〜NFR-050 全件 / v1.0.0 / `features/v1-stabilization/` 完了. coverage-audit.md + test-results.md + release-checklist.md / auditor Pass. v1.0.0 タグ打鍵はドッグフーディング手動確認後 |
| BL-025 | E2E テスト基盤導入（Playwright + 全層疎通スモーク + CORS 修正） | P1 | Done | NFR-020 補強 / `feature/e2e-tests` ブランチで完了. Playwright + chromium 導入 (`playwright.config.ts`, `e2e/smoke.spec.ts`), `hono/cors` を `server/src/app.ts` の authMiddleware 前に追加, 「タスクを追加すると今日の一覧に表示される」スモークテストが green. このスモーク 1 件で CORS preflight / env 配線 / 認証 / migration 適用 / DB 永続化 / UI 再描画の全層が同時に検証される |
| BL-026 | 機能別 E2E スモークテスト群（happy path を実ブラウザで網羅） | P1 | Done | NFR-020 補強 / `feature/e2e-tests-functional` ブランチで完了. e2e/tasks.spec.ts (4 件: 優先度/期限切替/編集/削除) + e2e/projects.spec.ts (1 件: カスケード null) + e2e/trash.spec.ts (2 件: 復元/空にする) + e2e/settings.spec.ts (1 件: 境界時刻更新) + e2e/routines.spec.ts (2 件: 作成/削除) を追加. 既存 smoke を含めて合計 11 件 全 pass (5.3s). ルーティンの「翌日タスク自動生成」と境界時刻変更が `/today` に与える挙動の検証は時間制御 hook が必要なため BL-027 に移管 |
| BL-027 | クロスレイヤ整合性 E2E テスト群（Playwright 側で書ける範囲） | P2 | Done | `feature/e2e-tests-crosslayer` ブランチで完了. e2e/state-restoration.spec.ts (3 件: 完了数/focus/settings のリロード復元) + e2e/idempotency.spec.ts (1 件: 同 Idempotency-Key の 2 回目応答再生) + e2e/offline-queue.spec.ts (1 件: offline 起票 → online で flush) を追加, 合計 5 件 green. **2 タブ同時編集 ConflictDialog / 401 UI 反応 / offline PATCH の 3 項目は実装側に既存バグがあり E2E で「現状で起きないこと」を testing する形になるため BL-031 (web app の conflict/error handling 修復) に切り出した** |
| BL-028 | PWA / Service Worker E2E テスト (dev で testable な範囲) | P2 | Done | `feature/e2e-tests-pwa` ブランチで完了. e2e/pwa.spec.ts 2 件: (a) `/manifest.webmanifest` が name/short_name/start_url/display/icons (192/512) の PWA installable 要件を満たす, (b) `/dev-sw.js?dev-sw` が precache / NavigationRoute / StaleWhileRevalidate / 書込キュー sync の 4 機能を含む. **SW の実 activated 検証 / オフライン navigation / Lighthouse 監査 / install prompt / 更新 prompt** は Playwright 1.60 のデフォルト headless が `chromium-headless-shell` を使い SW が起動しないため検証不能. これらは BL-032 で full Chromium + prod build による別 Playwright project を立てて対応する |
| BL-029 | アクセシビリティ・パフォーマンス E2E テスト（任意） | P3 | Done | NFR-010 補強 / `feature/e2e-tests-a11y-perf` ブランチで完了. `@axe-core/playwright` を導入. e2e/a11y.spec.ts (5 件: today/projects/trash/routines/settings で WCAG 2.1 AA 違反 0 件) + e2e/keyboard.spec.ts (1 件: Tab + 入力 + Enter のみでタスク追加) + e2e/perf.spec.ts (1 件: 1000 件タスク投入下で `/today` が 13 ms 応答 → 1 秒以下を余裕でクリア) を追加. E2E 25 件 全 green. 「編集 / 完了がキーボードのみで完結」は ARIA 設計の見直しが必要なため最低限の起票のみカバーした |
| BL-030 | E2E テスト用 server 側 hook の整備（時間制御 + force reset + 再起動シミュレーション） | P2 | Done | `feature/test-hooks` ブランチで完了. `TEST_NOW` 環境変数で `FakeClock` を注入し, `/api/v1/test/clock/{,set,advance}` の 3 エンドポイントで時刻を任意に進められるようにした (idempotency middleware は `/api/v1/test/` を除外). これを使った e2e/boundary-time.spec.ts (3 件: 未完了繰越 / カウンタリセット / ルーティン自動生成) を追加. **force reset エンドポイントは clock advance で代替したため不要だった**. **server 再起動シミュレーションは Playwright で server プロセスを kill/restart する仕組み構築が大ごとで, migration 冪等性は BL-025 で手動検証済みのため ROI が低く本 BL からは外した** (必要が出た時は別 BL で扱う) |
| BL-031 | Web アプリの conflict / error handling UI 修復（today-view 部分対応） | P2 | Done | `feature/web-error-handling` ブランチで完了 (today-view の中核 mutation のみ). (a) **online 412 → ConflictDialog**: `today-view.tsx` の update/delete/complete 3 mutation で `OptimisticLockError` を catch して `findEntryByKey()` で queue 内 entry を引き `ConflictError(entry, error.currentTask)` に変換するように修復. (b) **offline PATCH の If-Match 欠落**: 同 4 mutation (update/delete/complete/setFocus) で `If-Match` を header に乗せるよう変更し body から ifMatch を除去. E2E (e2e/conflict-handling.spec.ts) で「2 タブ同時編集 → ConflictDialog 出現」「offline PATCH → online flush → server 反映」が green. **未対応 (BL-033 に切り出し)**: 同様の (a) 修復を projects/routines/trash/settings の各 view にも適用 (それぞれ独自の Conflict 系エラー型を持つ). **(c) 401 / ネットワークエラー時の UI 反応**: UI 設計判断 (toast / banner / 静かな refetch) を要するため BL-034 に切り出し |
| BL-033 | conflict 変換修復を他 view にも展開 (BL-031 の残り) | P3 | Done | `feature/web-error-handling-other-views` ブランチで完了. `offline-queue.ts` に汎用ヘルパー `mapConflict(idempotencyKey, fn, extractServer)` を追加. project-repository.ts / routine-repository.ts に 412 ハンドリングと `ProjectConflictError` / `RoutineConflictError` を新設. projects-view / routines-view / trash-view の update/delete/restore 計 5 mutation で `mapConflict` 経由で ConflictError に変換するよう統一. E2E に「2 タブ同時編集でプロジェクト名衝突 → ConflictDialog」を追加. settings-view は既存 PatchConflictError で state を直接更新する設計のため意図的に dialog 経路には乗せず据え置き |
| BL-034 | 401 / ネットワークエラー時の UI 反応 (BL-031 (c) から切り出し) | P3 | Done | `feature/error-notification` ブランチで完了. **設計**: `OfflineBanner` と並ぶ短時間 auto-dismiss バナー (5 秒で自動消滅 + × で手動 dismiss). モーダルにせず user 操作をブロックしない. グローバル state は React Context を避け `web/src/error-notification.ts` の module-level pub/sub (`notifyError` / `useErrorNotification` / `dismissError`) で実装. **適用**: today / projects / routines / trash の 4 view 全 12 mutation の `onError` で ConflictError 以外を `notifyError("通信に失敗しました")` に流す統一. main.tsx で `<ErrorNotification />` を `<OfflineBanner />` と並べてマウント. **E2E**: `page.route` で 401 を返し, バナー (`role="alert"`, `aria-label="通信エラー通知"`) が表示されることを確認 |
| BL-032 | PWA E2E テストの full Chromium + prod build 対応 | P3 | Done | `feature/pwa-prod-build` ブランチで完了. playwright.config.ts に `chromium-pwa` project を追加し full Chromium バイナリ (`~/.cache/ms-playwright/chromium-*/chrome-linux64/chrome`) を `launchOptions.executablePath` で強制. 同時に prod build 用 webServer (`npm run build -w web && npm run preview -w web`, port 4173) を追加し, chromium-pwa project はこの port を baseURL に使う. e2e/pwa-prod.spec.ts に 2 件: (a) SW が registration → activated に到達, (b) activate 後にオフラインで reload しても NavigationRoute が cached `/index.html` を返して画面が描画される. 未対応 (別 BL に切り出し): Lighthouse PWA 監査スコア assert (lighthouse npm package 導入要), `beforeinstallprompt` 発火検証 (headless では不能), SW 更新通知 (prompt 戦略) の自動再現. dev モードの SW は `/dev-sw.js` の module 解決周りで script evaluation が失敗するため testable にせず, prod build を testable 経路として採用した |
| BL-035 | UI 全面刷新の総合仕様 (デザイン基盤と差分カタログの確定) | P0 | Done | NFR-010, NFR-013 / UI 全面刷新ロードマップ / 想定 `features/ui-redesign-foundation/`. モックアップ (`local/image.png`) は手書きワイヤフレームで, 上下 2 領域に「現在のタスク」(タスクなし時も枠あり, 下部に削除/完了の 2 ボタン) と「今日のタスク」(左サイドナビ + メイン: タイトル, 右上の "+プロジェクトの追加" ボタン, プロジェクトトグル + タスク名入力 + 星 3 つの優先度 + 追加ボタンの起票フォーム, 1 件のタスクカードに削除/明日にする/完了) が並ぶ縦長レイアウト. 本 BL では UI 刷新全体のスコープ・ナビ構造・タイポグラフィ/カラーの基本方針・既存 view との差分カタログを spec.md にまとめ, 後続 BL の前提を確定する. 完了の目安は spec/plan が承認され, 後続 BL-036 以降が独立に着手可能になること |
| BL-036 | 左サイドバー導入と 3 ビュー切替 (現在/今日/明日) | P0 | Done | NFR-010, NFR-013 / BL-035 依存 / 想定 `features/ui-sidebar-nav/`. 対象 `web/src/main.tsx` (Routes), 新規 `web/src/ui/app-shell/`. モックでは左に縦並びで「現在のタスク」「今日のタスク」「明日のタスク」の 3 リンクが配置され, 右がメインビュー. 現状は `/today` 1 本に focus + 一覧が同居している. 本 BL では `/focus` `/today` `/tomorrow` の 3 ルート + 共通 AppShell (サイドバー + 右メイン) を導入し, 既存 view を 3 つに分解する箱だけ用意する. 設定/ゴミ箱/ルーティンへの導線はサイドバー下部の補助メニューにまとめる. 完了の目安は 3 ルート間遷移が E2E で通ること |
| BL-037 | 「現在のタスク」独立ビュー化 (focus-view) | P0 | Done | FR-012, FR-013, NFR-011 / BL-036 依存 / 想定 `features/focus-view/`. 対象 新規 `web/src/ui/focus-view/`, 既存 `today-view.tsx` の focus セクションを移植. モックではタスクなし時も角丸の枠が大きく表示され, 下部に「削除」「完了」の 2 ボタンのみ. アクションは現状 6 種類 (優先度切替/編集/明日へ/完了/削除/現在解除) から 2 種類 (削除/完了) へ削減. focus 解除は明示ボタンではなく完了/削除時の自動繰上げ (BL-006 ロジック) で行う. 編集や優先度変更は今日ビューに戻ってから行う前提. 完了の目安は focus 表示 + 完了 + 削除の 3 経路 E2E green |
| BL-038 | 「明日のタスク」独立ビュー (tomorrow-view) | P0 | Done | FR-005, FR-014 / BL-036 依存 / 想定 `features/tomorrow-view/`. 対象 新規 `web/src/ui/tomorrow-view/`, サーバ側は既存 `/api/v1/tasks?dueDate=tomorrow` を再利用 (なければ補強). 現状 dueDate=tomorrow のタスクは今日ビューから「明日へ」操作で押し出すのみで一覧表示はできない. 本 BL では明日分の一覧 + 起票 (期限固定で tomorrow) + 「今日にする」(明日→今日への移送) を実装する. 優先度順表示は今日ビューと同じ. 完了の目安は明日タスクの起票・移送・削除が E2E で通ること |
| BL-039 | 起票フォームから期限セレクトを削除 (ビュー文脈で dueDate 決定) | P1 | Done | FR-005 / BL-037, BL-038 依存 / 想定 `features/inline-create-form/`. 対象 `web/src/ui/today-view/today-view.tsx` (起票フォーム), `tomorrow-view/`. モックの起票フォームには期限セレクトがなく, プロジェクトトグル + タスク名 + 星 + 追加 の 4 要素のみ. 今日ビューで起票したものは `dueDate=today`, 明日ビューで起票したものは `dueDate=tomorrow` を強制する仕様に変更する. 「現在のタスク」ビューには起票フォームを置かない. 完了の目安は各ビューでの起票結果が想定 dueDate を持つことを E2E で確認 |
| BL-040 | 優先度 UI を星 3 つの評価式に変更 | P1 | Done | FR-003, FR-004 / BL-035 依存 / 想定 `features/priority-star-ui/`. 対象 起票フォーム + タスクカードの優先度表現. モックでは ☆☆☆ + 「↑タップで選択」のラベルで星評価 UI を採用 (☆3=最優先, ☆2=普通, ☆1=後回し のマッピング想定. 詳細は spec で確定). 現状は `<select>` の「最優先/普通/後回し」3 値. 起票フォームの優先度 input と, タスク一覧上の優先度表示・切替の両方を星に置き換える. ドメイン値 (`Priority` 型の 3 値) は変更しない. 完了の目安は星クリックで priority が正しく送信され, 既存サーバ API は無改修で動くこと |
| BL-041 | プロジェクト選択をトグル UI に変更 (起票フォーム) | P1 | Done | FR-020 / BL-035 依存 / 想定 `features/project-toggle-ui/`. 対象 起票フォームのプロジェクト input. モックでは「プロジェクト名 (トグルで選択)」と注記された横長ボタン群でプロジェクトをサイクル切替する想定. 現状は `<select>` で全プロジェクトをドロップダウン表示. プロジェクト数が少ない (個人利用) 前提でトグル UI に置き換える. プロジェクト未選択 (null) もトグルの 1 状態として扱う. 完了の目安はトグルで全プロジェクト + null が一巡できて, 起票時に projectId が正しく送信されること |
| BL-042 | タスクカードのアクションを 3 つに削減 (削除/明日にする/完了) | P1 | Done | FR-005, FR-006, FR-007 / BL-035, BL-040 依存 / 想定 `features/task-card-actions/`. 対象 `web/src/ui/today-view/today-view.tsx`, `tomorrow-view/`. モックでは各カードに「削除」「明日にする」「完了」の 3 ボタンのみで, 現状の 6 ボタン (優先度切替/編集/明日へ/完了/削除/現在に設定) を簡素化. 優先度変更はカード上の星 UI (BL-040) でタップ, 名称編集はカードタップ→ダイアログ起動 (詳細別 BL), 「現在に設定」は別経路に切り出し (BL-043). 「明日にする」ボタンは today ビューでのみ表示し, tomorrow ビューでは「今日にする」表示にする. 完了の目安は 3 アクションのみで操作系が完結すること |
| BL-043 | 「現在に設定」操作の導線再設計 (カード長押し or 別アクション) | P2 | Done | FR-012 / BL-037, BL-042 依存 / `features/set-focus-gesture/` 完了. UX は spec で「タスクカード上の常時表示 `<button>`『現在のタスクにする』」に確定 (長押し/コンテキストメニューはタッチ・キーボード両立性で不採用. PriorityStars と同じ状態系コントロール分類でアクション 3 ボタンのカウント外). ネイティブ button により Tab + Enter のキーボード経路も単一実装で成立. 解除 UI は恒久的に非提供 (FR-013 の自動解除のみ). tomorrow ビューは対象外 (サーバが 400 INVALID_FOCUS_TARGET). 実装は `today-view.tsx` のみ (setFocusMutation 再導入 + 失敗時 `["focus"]` invalidate で 412 後の再試行を可能に). e2e/set-focus-gesture.spec.ts (AC-1〜AC-9) + state-restoration の skip 解除 1 件で検証, auditor Pass |
| BL-048 | リポジトリ全体の lint / typecheck の修復（main 時点から失敗している） | P2 | Done | 開発基盤 / BL-043 の auditor 監査で発覚. **lint**: `npm run lint` (biome check) が 349 errors で失敗. 内訳は診断 6,700 件超のうち約 4,440 件がコミット済み Capacitor ビルド成果物 (`android/app/src/main/assets/public/assets/index-*.js` 4,259 件 + `service-worker.js` 183 件) への診断で, biome の ignore 対象に成果物を追加するのが本筋 (そもそも成果物をコミットし続けるかも要検討). 残り約 150 件は手書きコード (web のテスト・src) の organizeImports / format / noNonNullAssertion 等で, 大半は `biome check --write` で機械修正可能. **typecheck**: `npm run typecheck` (tsc -b --noEmit) が tsconfig 構成問題で失敗. TS6310 (server/web が参照する domain が project references なのに emit 無効) と TS6059 (domain の `__tests__/` が rootDir `src` の外) の 2 種. composite + declaration emit への変更か, テスト用 tsconfig 分離が必要. 完了の目安は `npm run lint` と `npm run typecheck` が exit 0 になり, 各 feature の tasks.md にある「lint / typecheck が通る」チェックが実態を持つこと. CI gate 化は別判断 |
| BL-044 | "+プロジェクトの追加" ボタンを今日ビューに配置 | P2 | Done | FR-020 / BL-035 依存 / `features/inline-project-create/` 完了. 今日ビュー見出し領域に「＋プロジェクトの追加」ボタンを配置し, ネイティブ `<dialog>` + `showModal()` のモーダル (新規 `web/src/ui/project-create-dialog/`) で名称 1 件を受け取り `ProjectRepository.create()` を呼ぶ. 成功時は `["projects"]` invalidate でトグル UI (BL-041) に即時反映 + 新規プロジェクトを起票フォームに自動選択 (オンライン時のみ). 失敗時は notifyError + 入力保持で再試行可能. オフラインは既存 offline-queue 同型で enqueue. tomorrow/focus には置かず, ProjectsView・サーバ・Repository は無改修. E2E 11 件 (AC-1〜AC-11) + a11y モーダル展開スキャン + 単体 5 件で検証, auditor Pass. ProjectsView 側作成フォームとの導線重複の整理は別 BL 候補 (spec U-2) |
| BL-045 | 既存 view (settings/trash/routines/projects) のサイドバー統合とスタイル統一 | P2 | Done | NFR-010 / BL-036 依存 / `features/secondary-views-shell/` 完了. BL-036 でサイドバー導線・AppShell 配下レンダリングは実装済みのため, 本 BL は CSS 追加 + className 付与のみ. 各 view に BEM ルートクラス + 専用 CSS (H1 24px / 角丸枠フォーム 12px / 角丸カードリスト / 空状態 #595959 中央寄せ) を新規追加. trash-view は `<header>` 化で H1 と「ゴミ箱を空にする」を同居させ today-view の header 慣行と統一. 全暫定値に `TODO(BL-046)` マーカー付き. 機能差分なし. E2E 6 件 (AC-2〜AC-7) + 全既存テスト green. auditor Pass |
| BL-046 | デザイントークン / CSS 基盤の整備 (タイポ・余白・角丸・カラー) | P2 | Done | NFR-010 / BL-035 依存 / 想定 `features/design-tokens/`. モックは手書きで角丸の大きな枠・余白の広いカード・大きなタイトル文字を採用しており, 現状の素の HTML スタイルとは大きく異なる. 本 BL ではデザイントークン (CSS variables) を定義し, 全 view で参照する基盤を整える. CSS フレームワーク選定 (Tailwind / vanilla CSS / CSS Modules) は ADR で確定する. 完了の目安はトークン定義ファイルが配置され, 後続 BL-036〜BL-045 が同じトークンを参照できる状態になること |
| BL-047 | 完了タスク数カウンタの配置見直し (モック非表示問題) | P3 | Done | FR-040 / BL-035 依存 / `features/completion-counter-placement/`. BL-035 U-003 の案 (c)「today-view 見出し右に小さく表示」を採用. `<header>` 内の h1 と「＋プロジェクトの追加」ボタンの間に `<span aria-label="今日の完了タスク数">` を配置し, `today-view.css` 新規作成でデザイントークン (`--font-size-small` / `--color-fg-subtle`) を適用する. サーバ API / データモデル / Repository は無改修. 完了の目安は新配置で counter が表示され, 既存テスト (BL-008) が green を維持すること |
| BL-049 | サイドバーをハンバーガーメニューに変更 | P2 | Done | NFR-010 / BL-036 依存 / `features/hamburger-nav/` 完了. ハンバーガーボタン (☰) で開閉するオーバーレイメニューに置き換え, `<main>` が全幅を占有. ナビ項目構成 (プライマリ 3 件 + セカンダリ 4 件) は維持. menuOpen=true で `role="dialog"` / `aria-modal="true"` / `aria-label="ナビゲーションメニュー"` を付与. Escape / overlay click / リンク選択で自動 close + ハンバーガーボタンに focus 復帰 (REQ-12 / REQ-13). 実装本体 (app-shell.tsx / app-shell.css) は過去 BL-049 で完了済み. finalize 作業 (feature/hamburger-nav-finalize ブランチ): (a) AC-1〜AC-9 を検証する新規テスト `web/__tests__/hamburger-nav.test.tsx` 7 件 (AC-1/2/3/4/5/7/9, AC-6 全幅 / AC-8 アクティブスタイルは jsdom 限界で E2E 委譲) + `e2e/hamburger-nav.spec.ts` 9 件 (AC-1〜AC-9 全件) を新規追加. (b) BL-049 由来で失敗していた既存 E2E 16 件 (sidebar-nav 8 / focus-view 3 / tomorrow-view 3 / set-focus-gesture 2) のヘルパに `openHamburger()` 経由のナビゲーション操作を追加. 合わせて tomorrow-view / set-focus-gesture で BL-051 の `<section aria-label="明日のタスク">` → `<main>` 統合へのランドマーク追従 (region locator → main) を 3 行修正. 検証: vitest 55 files / 833 passed + 1 skipped + playwright 85 passed / 3 skipped / 0 failed + lint 既存 4 warnings のみ + typecheck exit 0 + auditor PASS |
| BL-050 | today ヘッダの「＋プロジェクトの追加」ボタン撤去 | P3 | Done | NFR-010 / BL-044, BL-049 依存 / `features/remove-inline-project-create/` 完了. today ビュー header から「＋プロジェクトの追加」 button + `projectDialogOpen` state + `<ProjectCreateDialog>` マウント + import の 4 箇所を撤去. `e2e/inline-project-create.spec.ts` を削除し, 代わりに `e2e/remove-inline-project-create.spec.ts` で「ボタン非存在」「/projects 経路で作成」「トグルへの反映」を 6 シナリオで検証. `e2e/a11y.spec.ts` の BL-044 関連 a11y スキャンブロックも併せて削除. `ProjectCreateDialog` コンポーネント本体は `/projects` 側で利用継続 (今は projects-view.tsx が独自実装のため未利用だが本体は温存). 単体 730 passed / 1 skipped + a11y E2E 7/7 + 新規 E2E 6/6 + lint / typecheck green / auditor Pass. **検出された既存リグレッション** (BL-049 由来, BL-050 とは無関係): main HEAD でも `npx playwright test` 全件実行で 15 件失敗. sidebar-nav / focus-view / tomorrow-view / set-focus-gesture 系の各 spec が「ナビゲーション操作前にハンバーガーメニューを開く」ステップを持たずリンクが viewport 外で click 失敗する. 別 BL で修復が必要 |
| BL-051 | 今日/明日ビューの構造整理 (共通レイアウト化) | P2 | Done | NFR-010 / BL-036, BL-038, BL-049, BL-050 依存 / `features/unified-day-view/` 完了. today/tomorrow の HTML 構造とクラス体系を `day-view__` 名前空間に統一. 新規 `web/src/ui/day-view/day-view.css` に共通構造クラス (display / flex / gap / list-style / h1 font-size のみ。visual 詳細は BL-052) を集約. today-view.tsx は `<main className="day-view">` + `.day-view__header` + 起票 form + `.day-view__card .day-view__card--focus` (現在のタスク) + `.day-view__list` の縦並び. tomorrow-view.tsx も同形で「現在のタスク」のみ無し. 旧 tomorrow-view.css は全削除し, today-view.css は `.today-view__completion-count` のみ残存. 追従修正: BL-046 の `design-tokens.test.ts` `TARGET_CSS_FILES` から削除ファイル参照を除去. vitest 749 passed / 1 skipped + lint / typecheck green + auditor PASS |
| BL-052 | タスクカードのデザイン統一 (縁・余白・角丸) | P2 | Done | NFR-010 / BL-051 依存 / `features/task-card-design/` 完了. `web/src/ui/day-view/day-view.css` の `.day-view__card` に `background: var(--color-bg)` / `border: 1px solid var(--color-border)` / `border-radius: var(--radius-md)` / `padding: var(--space-md)` を追加し, カードとしての縁・背景・角丸・余白を確立. 「現在のタスク」用の強調 variant `.day-view__card--focus` には `border-width: 2px` / `border-radius: var(--radius-lg)` / `padding: var(--space-lg)` を上書き設定 (border-color / background は通常カードを継承). tokens.css は無改修, shadow / hover / transition は意図的に追加せず border を主役にした visual. focus-view (`/focus`) も対象外. 単体 780 passed / 1 skipped + 新規 task-card-design.test.ts 31 件 + lint / typecheck green + auditor PASS |
| BL-053 | ハンバーガーボタンと h1 タイトルの重なり修正 | P2 | Done | NFR-010 / BL-049 フォローアップ / `features/hamburger-overlap-fix/` 完了. 採用案 A (`.app-shell__main` への `padding-top: calc(var(--space-md) + var(--space-xl))` = 48px 追加). 既存 `padding: var(--space-md)` の左右下端を維持しつつ上端のみ上書きすることで, ハンバーガーボタン (約 36px) と各 view の `<h1>` の重なりを解消. app-shell.tsx / 各 view tsx / tokens.css は無改修. 新規 `hamburger-overlap-fix.test.ts` (10 件) で `.app-shell__main` の padding-top 宣言を CSS 文字列 assert で検証. 単体 790 passed / 1 skipped + BL-049 既存 18 件回帰なし + lint / typecheck green + auditor PASS |
| BL-054 | 起票フォーム (.day-view__form) のカード化 | P2 | Done | NFR-010 / BL-051, BL-052 依存 / `features/form-card-design/` 完了. `web/src/ui/day-view/day-view.css` の `.day-view__form` に `.day-view__card` と同値の visual 4 宣言 (`background: var(--color-bg)` / `border: 1px solid var(--color-border)` / `border-radius: var(--radius-md)` / `padding: var(--space-md)`) を追加し, 起票フォームをタスクカードと同じ視覚言語のカードとして統一. 既存の構造系宣言 (display / flex-direction / gap) は維持し, 順序は構造系 → visual で揃える. CSS 値は `.day-view__card` と二重に書くが BL のスコープを CSS 1 ファイルに閉じるためクラス併記による DRY 化は不採用 (将来 BL の余地). tokens.css / JSX / focus-view.css は無改修. shadow / hover / transition / animation は BL-052 と同方針で不採用. 追従修正: BL-052 `task-card-design.test.ts` AC-7 `OTHER_SELECTORS` から `.day-view__form` を除外 (本 BL で visual を意図的に追加するため). 新規 `web/__tests__/form-card-design.test.ts` (37 件) で AC-1〜AC-10 を CSS 直読みで機械検証. 単体 826 passed / 1 skipped + a11y E2E 7 件 violations 0 + lint / typecheck green + auditor PASS |
| BL-056 | プロジェクト名 chip スタイル新設 + タスクカードへのプロジェクト名表示追加 | P2 | Done | NFR-010 / BL-035, BL-041, BL-046 依存 / `features/project-chip/` 完了. `web/src/ui/day-view/day-view.css` の末尾に共通クラス `.project-chip` (border / border-radius / padding / font-size / color の 5 宣言. 既存トークン `--color-border` / `--radius-lg` / `--space-xs` / `--space-sm` / `--font-size-small` / `--color-fg` のみ利用) を新設. tokens.css は無改修 (D-003 で `--radius-pill` 新設は不採用 / `--radius-lg` を流用). `today-view.tsx` の focusedTask セクションと otherTasks `<li>` の 2 箇所で `{project && <span className="project-chip">{project.name}</span>}` をタスク名 span の直前に挿入 (projectId === null は省略 / D-002). `tomorrow-view.tsx` も同パターン. `<ProjectToggle />` の button className に `project-chip` を**追加** (既存 `project-toggle__button` は併記で残存 / D-004). project-toggle.css / focus-view.css / その他既存 view は無改修. 新規 `web/__tests__/project-chip.test.tsx` (36 件) で AC-1〜AC-13 を CSS 直読み + jsdom DOM レンダの両系統で機械検証. 検証: vitest 56 files / 869 passed | 1 skipped (BL-049 finalize ベース 833 → +36) + a11y E2E 7 件 violations 0 + lint / typecheck green + auditor PASS. BL-057 (3 段ゾーン化) / BL-058 (2D グリッド) の先行基盤として完了 |
| BL-057 | タスクカード (.day-view__card) のレイアウトを 3 段ゾーン構造へ刷新 | P2 | Done | NFR-010 / BL-035, BL-042, BL-043, BL-052, BL-056 依存 / `features/task-card-zone-layout/` 完了. `.day-view__card` を `flex-direction: column` + `align-items: stretch` + `border-radius: var(--radius-lg)` (D-001 流用 / tokens.css 無改修) に変更し, 内部に 3 つの新クラス `.day-view__card__header` (chip 配置) / `.day-view__card__title` (タスク名 + 星 / `justify-content: space-between`) / `.day-view__card__actions` (`justify-content: flex-end` + `flex-wrap: wrap`) を追加. today-view (focusedTask section + otherTasks li) / tomorrow-view (li) の JSX を NFR-DOM-ADDITIVE (= 既存子要素を削除/改名せず 3 つの `<div>` で囲うだけ) で 3 段化. 「現在のタスクにする」(BL-043) は D-002 で actions 内に同居 (= today 側は 4 ボタン化, `flex-wrap` で吸収). `<PriorityStars />` (BL-040) は D-003 で title 内に配置. focus-view (`/focus`) / tokens.css は完全無改修. 追従修正: BL-052 task-card-design.test.ts AC-1 (`align-items: center` / `border-radius: var(--radius-md)` を BL-057 値に追従) + BL-054 form-card-design.test.ts AC-7 + BL-056 project-chip.test.tsx AC-7 の `.day-view__card` 不変性 assert を `--radius-lg` に追従. 既存 E2E 4 ファイル (tasks / state-restoration / trash / project-toggle) の `taskRow()` helper を `locator("..")` → `locator("xpath=ancestor::li")` に追従 (D-007 の想定が DOM 階層変更で外れたため). 新規 `web/__tests__/task-card-zone-layout.test.tsx` (47 件) で AC-1〜AC-15 を CSS 直読み + jsdom DOM レンダで網羅. 検証: vitest 57 files / 916 passed | 1 skipped (BL-056 baseline 869 → +47) + Playwright 85 passed / 3 skipped / 0 failed + lint / typecheck green + auditor PASS |
| BL-058 | 起票フォーム (.day-view__form) のレイアウトを 2D グリッド配置へ刷新 | P2 | Done | NFR-010 / BL-035, BL-039, BL-040, BL-041, BL-054, BL-056 依存 / `features/task-form-grid-layout/` 完了. `.day-view__form` を `display: grid` + `grid-template-areas: "project priority" "name name" ". submit"` + `grid-template-columns: 1fr auto` + `gap: var(--space-md)` に刷新. BL-054 visual 4 宣言 (background / border / border-radius / padding) は完全保持. 新規 5 子クラス `.day-view__form__project` / `__priority` / `__priority-hint` / `__name` / `__submit` に `grid-area` 割り当て. submit は `justify-self: end` で明示右寄せ. PriorityStars (BL-040) の直下に `<span className="day-view__form__priority-hint">↑タップで選択</span>` を新規追加 (D-002: span + 外置き / PriorityStars 本体無改修 / D-003: aria-describedby 不採用). today-view と tomorrow-view は個別書き換え (D-007: 共通 component 化は将来 BL). focus-view / tokens.css / PriorityStars / ProjectToggle / `.day-view__card` 系 / `.project-chip` はすべて無改修. NFR-DOM-ADDITIVE で既存子要素・コンポーネント API は無改修, ラッパー追加のみ. 追従修正: BL-054 form-card-design.test.ts AC-2 の flex 系 assert を grid 系に置換 (5 件 / P-006). 新規 `web/__tests__/task-form-grid-layout.test.tsx` (78 件) で AC-1〜AC-14 / AC-18 を CSS 直読み + jsdom DOM レンダで網羅. 検証: vitest 996 passed | 1 skipped (BL-057 baseline 916 → +81) + Playwright 85 passed / 3 skipped / 0 failed (E2E は無修正で通る = D-007 想定が裏付けられた) + lint / typecheck green + auditor PASS |
| BL-059 | TaskCard / TaskFormCard コンポーネント新設 + モックアップ通りの visual 確定 | P2 | Done | NFR-010 / BL-035, BL-040, BL-041, BL-043, BL-056, BL-057, BL-058 依存 / `features/task-card-component/` 完了. 新規 React コンポーネント `<TaskCard>` (タスク表示) と `<TaskFormCard>` (タスク起票) を `web/src/ui/task-card/{task-card,task-form-card}.tsx` に新設 + ペア専用 CSS `web/src/ui/task-card/task-card.css` に `.task-card` / `.task-card--focus` / `.task-card__header` / `.task-card__title` / `.task-card__actions` を集約. today / tomorrow / focus の 3 ビューで `<TaskCard>` を再利用 (focus は `variant="focus" actionSet="minimal"` で 2 ボタン制約維持 / D-007). 起票フォーム箇所も `<TaskFormCard>` に置換. variant / actionSet / showPriority / showSetFocus / dueDateMode / as の prop で 4 ビュー差異を吸収 (D-002 / D-003). タスク名フォントは `--font-size-h2` 流用で tokens.css 無改修 (D-005). モックアップ visual 7 件確定: V-1 強調は border-width 3px (padding は通常と同じ `--space-md`) / V-2 actions 中央揃え / V-3 PriorityStars は header 段右 / V-4 タスク名は title 段で中央 + `--font-size-h2` / V-5 today-view から `<h2>現在のタスク</h2>` 撤去 (focus-view ルートの `<h1>現在のタスク</h1>` は維持) / V-6 起票カードから「↑タップで選択」「優先度」label span 撤去 / V-7 起票カード input にも `--font-size-h2`. 撤去: `.day-view__card` / `.day-view__form` 系 (day-view.css) と `.focus-view__card` (focus-view.css) を全撤去, 責務は `.task-card` 系に移譲. `.day-view__list` / `.day-view__empty` / `.project-chip` / `.focus-view` 枠 / `.focus-view__empty` は維持. PriorityStars (BL-040) / ProjectToggle (BL-041) / project-chip (BL-056) / tokens.css は無改修. 新規 `web/__tests__/task-card-component.test.tsx` (108 件) で AC-1〜AC-26 を CSS 直読み + jsdom DOM レンダ + view 適用 + 不変性 で網羅. 追従修正: BL-052 task-card-design / BL-057 task-card-zone-layout / BL-054 form-card-design / BL-058 task-form-grid-layout / BL-056 project-chip / BL-051 unified-day-view の 6 テスト + 波及で BL-046 design-tokens + E2E `e2e/design-tokens.spec.ts` + BL-045 `e2e/secondary-views-style.spec.ts`. 検証: vitest 59 files / 1059 passed | 1 skipped (BL-058 baseline 996 → +63) + Playwright 85 passed / 3 skipped / 0 failed (E2E は無修正で通った = `taskRow()` の `xpath=ancestor::li` と role + accessibleName ベースの設計が妥当) + lint / typecheck green + auditor PASS |
| BL-060 | ProjectCard / ProjectFormCard コンポーネント新設 + projects-view 適用 | P2 | Done | NFR-010 / BL-045, BL-059 依存 / 想定 `features/project-card-component/`. 現状 projects-view.tsx は `<li>` で直接プロジェクト名 + 「名称変更」「削除」を組み立て, 作成フォームは `<form className="projects-view__form">` で `<label>プロジェクト名</label><input /><button>追加</button>` の縦並び. 本 BL では新規 **`<ProjectCard>`** (プロジェクト表示) と **`<ProjectFormCard>`** (プロジェクト作成) を作り, projects-view に適用. ペア専用 CSS (`.project-card` / `.project-card__header` / `.project-card__title` / `.project-card__actions`) を `web/src/ui/project-card/project-card.css` に集約 (= TaskCard 系とは別 CSS / visual も独立). 同時に作成フォームを「プロジェクト名 input + 追加ボタン横並び」(タスク起票フォームの 2D グリッドと同じ配置パターン) に刷新. TaskCard 系 (BL-059) / RoutineCard 系 (BL-061) には touch しない. 完了の目安は projects-view が `<ProjectCard>` / `<ProjectFormCard>` で構成され, 作成フォームが横並びになり, 既存テスト追従後に全件 green |
| BL-061 | RoutineCard / RoutineFormCard コンポーネント新設 + routines-view 適用 | P2 | Done | NFR-010 / BL-045, BL-059 依存 / 想定 `features/routine-card-component/`. 現状 routines-view.tsx は `<li>` で直接ルーティン情報 + アクションを組み立て, 作成フォームも縦並び. 本 BL では新規 **`<RoutineCard>`** (ルーティン表示) と **`<RoutineFormCard>`** (ルーティン作成) を作り routines-view に適用. ペア専用 CSS (`.routine-card` 系) を `web/src/ui/routine-card/routine-card.css` に集約 (TaskCard 系 / ProjectCard 系とは別 CSS / visual も独立). 作成フォームも横並びパターンに刷新. TaskCard 系 / ProjectCard 系には touch しない. 完了の目安は routines-view が `<RoutineCard>` / `<RoutineFormCard>` で構成され, 作成フォームが横並びになり, 既存テスト追従後に全件 green |
| BL-062 | ハンバーガーボタンとオーバーレイメニューの重なり解消 | P2 | Done | NFR-010 / BL-049 フォローアップ / 想定 `features/hamburger-collapse-on-open/`. 現状 BL-049 で導入したハンバーガーボタン (`.app-shell__hamburger`) は `aria-label` こそ `menuOpen ? "メニューを閉じる" : "メニューを開く"` で切り替わるが, ☰ アイコンの button 自体は常時画面左上に **`position: fixed`** で表示され, オーバーレイメニュー (`.app-shell__menu--open`) が開いた状態でも見えたまま (= ☰ がメニューパネルと重なって見える). 本 BL ではメニューが開いている間 (menuOpen === true) はハンバーガーボタンを視覚的に隠す方向に統一する. 採用案は spec で確定するが候補は (a) `display: none` で完全非表示 + メニュー内に「閉じる」ボタンを別途配置, (b) `.app-shell__menu` の上に重ねて表示しメニュー右上の「×」アイコンに切り替え, (c) ハンバーガーボタンをメニューパネル内部に移動して常駐させる. a11y 要件 (Escape / overlay click / リンク選択での自動 close) は BL-049 で確定済みなので維持. 完了の目安はメニュー開状態でハンバーガーボタンの ☰ アイコンが視覚的に消える/メニューと重ならない, 既存 BL-049 の AC-1〜AC-9 が引き続き満たされる |
| BL-063 | TaskCard / TaskFormCard 実機遺漏の一括 hotfix | P2 | Done | NFR-010 / BL-059 hotfix / `features/task-card-hotfix/` 完了. 5 件の修正を CSS + JSX 微修正で実装: (1) `.task-card__header__priority { margin-left: auto }` で PriorityStars を chip 有無に関わらず常に右固定 (D-001) + `task-card.tsx` で PriorityStars を `<div className="task-card__header__priority">` wrap. (2) BL-059 V-2 (`justify-content: center`) を撤去し `.task-card__actions__delete { margin-right: auto }` / `.task-card__actions__complete { margin-left: auto }` の auto-margin パターンで「削除 左 / 中間 中央 / 完了 右」に置換, focus-view minimal でも 2 ボタン両端 (D-002). (3) `.task-card__header .project-chip { font-size: var(--font-size-small) }` の 2 class specificity で `.project-toggle__button` との competing を解消 (D-003). (4) `task-form-card.tsx` の label に `className="visually-hidden"` + `<input>` に `placeholder="タスク名"` 追加. `.visually-hidden` (9 宣言の標準パターン) + `::placeholder { color: var(--color-fg-subtle) }` を `task-card.css` 末尾に新設 (D-004). a11y は htmlFor + id 関連付け維持. (5) `.task-card--form .task-card__actions { justify-content: flex-end }` で起票カード「追加」を右端 (D-005). `vitest.config.ts` に `css: true` 追加 (jsdom getComputedStyle で margin-left: auto を観測するため). 追従修正: BL-059 task-card-component.test.tsx AC-5 + BL-057 task-card-zone-layout.test.tsx AC-3 + BL-058 task-form-grid-layout.test.tsx AC-7 で旧 V-2 `justify-content: center` assert を「撤去確認」に逆転 (D-007 / 計 3 ファイル). 新規 `web/__tests__/task-card-hotfix.test.tsx` (98 件) で AC-1〜AC-22 を CSS 直読み + jsdom DOM レンダで網羅. `<TaskCard>` / `<TaskFormCard>` の prop 型 / 各 view 呼び出し側 / PriorityStars / ProjectToggle / `.project-chip` / tokens.css はすべて無改修. BL-059 V-1 / V-3 / V-4 / V-5 / V-7 は維持, V-2 のみ置換. 検証: vitest 60 files / 1158 passed | 1 skipped (BL-059 baseline 1059 → +99) + Playwright 85 passed / 3 skipped / 0 failed (E2E 無修正) + lint / typecheck green + auditor PASS |
| BL-064 | TaskCard actions の DOM 順を「削除 → 現在のタスクにする → 明日にする → 完了」に変更 | P2 | Done | NFR-010 / BL-059 / BL-063 依存 / 想定 `features/task-card-actions-reorder/`. 現状 `task-card.tsx` の `<div className="task-card__actions">` 内 button 順序は (1) 「現在のタスクにする」 (showSetFocus + onSetFocus), (2) 「削除」 (`.task-card__actions__delete`), (3) 「明日にする」/「今日にする」 (showDueDateBtn), (4) 「完了」 (`.task-card__actions__complete`). BL-063 D-002 の auto-margin パターンで delete が `margin-right: auto` で左に押し出され, complete が `margin-left: auto` で右に押し出される設計だが, DOM 順が「現在のタスクにする → 削除」のため描画は `[現在のタスクにする][削除]──[明日にする][完了]` となり「現在のタスクにする」が左端に, 削除が左から 2 番目に表示されてしまう (= 期待と齟齬). 本 BL で DOM 順を「削除 → 現在のタスクにする → 明日にする → 完了」に変更し, auto-margin と組み合わせて `[削除]──[現在のタスクにする][明日にする]──[完了]` の配置を実現する. focus-view (`actionSet="minimal"`) は (削除, 完了) の 2 ボタンで影響なし (順序変更後も `[削除][完了]` で auto-margin により両端配置). 影響: `task-card.tsx` 1 ファイル (JSX 内の button 順序入れ替えのみ). CSS / props / 各 view 呼び出し側 / TaskFormCard は無改修. 既存 `task-card-component.test.tsx` / `task-card-hotfix.test.tsx` で DOM 順を assert している箇所があれば追従. 既存 E2E は role + accessibleName ベースのため無修正で通る想定. 完了の目安は 「削除」 が左端 / 「現在のタスクにする」と「明日にする」が中央 / 「完了」が右端に配置され, 既存テスト全件 green |
| BL-065 | 起票カードのプロジェクト選択を ProjectToggle から `<select>` に戻し ProjectToggle を撤去 | P2 | Done | NFR-010 / BL-041 撤去 / 想定 `features/project-toggle-removal/`. BL-041 (project-toggle-ui) で起票カードのプロジェクト選択を `<select>` から `<ProjectToggle />` (button トグル) に置き換えたが, user 評価で「ドロップダウンの方が一覧から選べて分かりやすい」と判定. 本 BL で `<ProjectToggle />` を `<select>` に戻し, ProjectToggle コンポーネント本体を撤去する. 影響: (1) `web/src/ui/task-card/task-form-card.tsx` で `<ProjectToggle ... />` を `<select id={...} value={projectId} onChange={...}><option value="">プロジェクトなし</option>{projects.map(...)}</select>` + visually-hidden label に置換, ProjectToggle import 撤去. (2) `web/src/ui/project-toggle/` ディレクトリ一式撤去 (本体 .tsx / .css). (3) 関連テスト 10 ファイルの追従: `web/__tests__/project-chip.test.tsx` AC-4 撤去, `task-card-component.test.tsx` AC-17 / AC-23 の ProjectToggle prop API 不変性 assert 撤去, `task-card-hotfix.test.tsx` AC-9 / AC-10 撤去, `task-form-grid-layout.test.tsx` AC-11 の ProjectToggle 関連 it 撤去, `today-view.test.tsx` / `tomorrow-view.test.tsx` の ProjectToggle 経由操作を `<select>` の `selectOptions()` 等に書き換え, `design-tokens.test.ts` の `project-toggle.css` 参照を撤去. (4) E2E: `e2e/project-toggle.spec.ts` 撤去, `e2e/projects.spec.ts` / `e2e/remove-inline-project-create.spec.ts` の helper を `<select>` 操作に書き換え. (5) `.task-card__header .project-chip` の specificity 強化ルール (BL-063 D-003) は TaskCard 表示側 (`<span className="project-chip">`) で引き続き必要なため維持. 完了の目安は起票カードで `<select>` が機能し ProjectToggle 関連コードが完全撤去され, 既存テスト追従後に全件 green |
| BL-066 | 起票カードのプロジェクト `<select>` (BL-065) の box サイズを縮小 | P2 | Done | NFR-010 / BL-065 依存 / 想定 `features/task-form-select-compact/`. BL-065 で `<ProjectToggle />` を `<select>` に置換した後, デフォルトの `<select>` (= ブラウザ既定スタイル + または `.project-chip` 継承) では box が大きく見える問題を解消する. `web/src/ui/task-card/task-card.css` に `.task-card__header select { ... }` を新設し, `min-height: 24px` (WCAG 2.5.8 AA タッチターゲット維持) / `padding` を縮小 / border / border-radius を chip と揃える / font-size は `--font-size-small` を維持. ベスト効果のため `appearance: none` で OS デフォルトの dropdown 矢印スタイルを統一する余地も spec で確定. 影響: `task-card.css` 1 ファイル追記. 他無改修. 完了の目安は起票カードのプロジェクト select の box 高さが現状の 44px から 24px (WCAG AA) 程度に縮小され, 視覚的に PriorityStars と並列の chip 風レイアウトに揃う + 既存単体 / E2E / a11y violations 0 件維持 |
| BL-067 | 共通ボタンスタイルの新設 (`.button` 系 CSS クラス) で全 view の button 見た目を統一 | P2 | Done | NFR-010 / 想定 `features/common-button-style/`. 現状 `web/src/ui/` 配下 13 ファイル (task-card / task-form-card / project-card / project-form-card / routines-view / trash-view / settings-view / setup-view / project-create-dialog / pwa-update-banner / error-notification / conflict-dialog / priority-stars) が `<button>` を直接記述し, スタイル統一が無く設計上のデフォルト + 各 view の親 CSS でアドホックに表示している. 「見た目の統一的変更」を 1 ファイルで完結できる基盤として **`.button` 共通 CSS クラス** を新設する (採用案 A / `<Button>` React コンポーネント化は不採用 = component 移行コスト回避). 新規 `web/src/styles/button.css` に `.button` 基底 (border / border-radius / padding / font-size / cursor / 標準 visual 等) と派生 `.button--primary` / `.button--danger` / `.button--ghost` 等を定義し, 13 ファイル各 `<button>` に `className="button"` (+ 必要なら派生 className) を当てる. 既存の専用クラス (例: `.task-card__actions__delete` / `.task-card__actions__complete` の auto-margin パターン / BL-063) は維持し button 配置制御として併記. 影響: 13 view tsx + 各 view CSS で既存 button スタイルが上書きしていれば撤去. 既存テスト追従は role + accessibleName ベースが大半なので最小化想定. 完了の目安は全 button が `.button` 基底スタイルを継承し, 以降のスタイル変更が CSS 1 ファイル変更で全 button に反映される + 既存単体 / E2E / a11y violations 0 件維持. variant (primary / danger / icon-only 等) の具体配分は spec で確定 |
| BL-068 | ルーティンの優先度 UI 統一 + 編集モードでの曜日変更 | P2 | Done | NFR-010 / BL-040, BL-061 依存 / 想定 `features/routine-card-edit-fields/`. 2 件の user 要求: (1) **優先度をタスクと統一**: `<RoutineFormCard>` の優先度 `<select id="routine-priority">` + `<label>優先度</label>` + `.routine-card__priority-row` ラッパを撤去し `<PriorityStars value={defaultPriority} onChange={onDefaultPriorityChange} groupLabel="優先度" idPrefix="routine-create" />` (BL-040) に置換. prop 型 `defaultPriority: string` を `defaultPriority: Priority` (`@todica/domain/task` 由来) に変更, `priorityId?` prop を撤去. BL-061 D-008-2 (優先度 label 可視維持) を逆転 (= radiogroup aria-label「優先度: ○○」で a11y 担保). (2) **編集モードで曜日変更**: `<RoutineCard isEditing={true}>` の form DOM に `<div className="routine-card__day-checkboxes" role="group" aria-label="曜日">` + 7 checkbox を追加 (DOM 順: input → 曜日 → 保存 → キャンセル). `RoutineCardProps` に `editingDaysOfWeek: number[]` / `onEditingDaysOfWeekChange: (next: number[]) => void` を追加, routines-view.tsx に `editingDaysOfWeek` state 新設 + `openEdit()` で初期化 + `cancelEdit()` でリセット + `handleSaveEdit()` で 0 件 silent return + `updateMutation` mutationFn 引数型に `daysOfWeek: number[]` を含めて offline-queue body と repository.update() の両方で送信. `WebRoutineRepository.update()` の `UpdateRoutineCommand` は BL-017 で既に `daysOfWeek?` を受理しており無改修. 影響: routine-form-card.tsx / routine-card.tsx / routine-card.css (`.routine-card__priority-row` / `.routine-card__select` の 2 空ルール撤去) / routines-view.tsx + 既存 `routine-card-component.test.tsx` (BL-061) の追従 20 件前後 + `e2e/routines.spec.ts` の優先度操作追従 (要 grep 確認) + BL-061 spec D-008-2 への注釈 1 行追加. 新規 `routine-card-edit-fields.test.tsx` (AC-1 〜 AC-25) + `e2e/routine-card-edit-fields.spec.ts` (4 シナリオ). 新規 component / 新規 CSS / tokens.css 改修なし. 完了の目安は (a) 起票カードの優先度が `<PriorityStars />` で表示 + Priority 型で送信, (b) 編集モードで曜日が変更可能 + daysOfWeek が PATCH に乗る (offline-queue body 含む), (c) BL-061 既存テスト全件 green + 新規テスト全件 green + a11y violations 0 件維持 |
| BL-069 | ルーティン編集モードでの優先度変更 | P2 | Done | NFR-010 / BL-040, BL-061, BL-068, BL-017 依存 / 想定 `features/routine-card-edit-priority/`. user 要求「ルーティンは優先度も変更できなくてはなりません」. BL-068 D-011 (= 編集モードの優先度変更は非ゴール) を本 BL で逆転. **採用方針**: `<RoutineCard>` の `isEditing=true` ブランチ form 内に `<PriorityStars value={editingDefaultPriority} onChange={onEditingDefaultPriorityChange} groupLabel="優先度" idPrefix="routine-edit" />` を追加. `RoutineCardProps` に `editingDefaultPriority: Priority` / `onEditingDefaultPriorityChange: (next: Priority) => void` を追加. DOM 順: input → 曜日 → 優先度 → 保存 → キャンセル (BL-068 D-005 を「優先度」追加で拡張). routines-view.tsx の state に `editingDefaultPriority` 追加 + `openEdit(routine)` で `setEditingDefaultPriority(routine.defaultPriority)` 初期化 + `cancelEdit()` で `"normal"` リセット + `handleSaveEdit()` で `updateMutation.mutateAsync({ ..., defaultPriority })` 送信. `updateMutation` mutationFn 引数型と offline-queue body に `defaultPriority: Priority` を追加. `WebRoutineRepository.update()` の `UpdateRoutineCommand.defaultPriority?: string` は BL-017 で対応済みのため無改修. `idPrefix="routine-edit"` で起票側 (BL-068 `"routine-create"`) と区別し id 衝突回避. **スコープ境界**: TaskCard / TaskFormCard / ProjectCard / RoutineFormCard / WebRoutineRepository / API / domain / server / 共通 button (BL-067) には触れない. **影響**: routine-card.tsx + routines-view.tsx + 既存 routine-card-edit-fields.test.tsx (BL-068 / 5 件前後) + routine-card-component.test.tsx (BL-061 / 3 件前後) + BL-068 spec D-011 への注釈 1 行追加. 新規 `routine-card-edit-priority.test.tsx` (AC-1 〜 AC-23) + `e2e/routine-card-edit-priority.spec.ts` (4 シナリオ). 新規 component / 新規 CSS / routine-card.css 新規セレクタ / tokens.css 改修なし. **完了の目安**: (a) 編集モードで優先度が `<PriorityStars />` で変更可能, (b) defaultPriority が PATCH に乗る (offline-queue body 含む), (c) BL-068 / BL-061 既存テスト全件 green + 新規テスト全件 green + a11y violations 0 件維持, (d) 起票/編集 PriorityStars の id 衝突 0 件 |
| BL-071 | RoutineCard ヘッダレイアウト刷新 (TaskCard と同じ 3 段構造に揃える) | P2 | Done | NFR-010 / BL-061, BL-068, BL-069, BL-070 依存 / 想定 `features/routine-card-header-layout/`. user 評価「ルーティン名が小さい + 優先度を右上にしたい」を解消する. **現状**: `<RoutineCard>` は `.routine-card` flex row + `.routine-card__main` flex column の 1 段 main 内に name input → 曜日 7 checkbox → PriorityStars を縦並びにし, 右に `.routine-card__actions`「削除」を置く構造 (BL-061 D-008 / D-012). name input のフォントは未指定 (= 親 body 既定). 優先度は main の 3 番目で右上ではない. **本 BL の方針**: TaskCard (BL-059 / BL-063) と同じ **3 段ゾーン構造** (`.routine-card__header` (name input + PriorityStars) / `.routine-card__day-checkboxes` (曜日 7 件) / `.routine-card__actions` (削除)) に揃える. (a) ヘッダ `display: flex` + `justify-content: space-between` で name input が左 / PriorityStars が右 (TaskCard 同イディオム). chip 不在のため `margin-left: auto` の小ワザは不要 (= `space-between` のみで右端固定が成立). (b) name フォントを `--font-size-h2` (= 20px / TaskCard と統一) に拡大. input は `font: inherit` で親から継承 (TaskCard V-7 と同じ). (c) 1 段 main 構造 (`.routine-card__main`) は撤去し, `.routine-card` 自体を `flex-direction: column` に変更して 3 段化する (= TaskCard と同じ機構). (d) 起票カード `<RoutineFormCard>` は `.routine-card--form` modifier で `flex-direction: column` を保ったまま 2 段構成 (1 段目 input + 追加 button / 2 段目 曜日 + PriorityStars) を維持 = 起票カードの体裁を壊さない. (e) 「削除」button (`.routine-card__actions`) は新構造下段に維持. **スコープ境界**: `<RoutineCard>` 本体 + `routine-card.css` のみ. TaskCard / TaskFormCard / ProjectCard / RoutineFormCard / WebRoutineRepository / API / domain / 共通 button (BL-067) / tokens.css には触れない. shadow / hover / transition / animation の追加禁止 (NFR-NO-SHADOW / NFR-NO-HOVER-TRANSITION). a11y (visually-hidden label `htmlFor=routine-name-{id}` / PriorityStars groupLabel `${routine.name} の優先度`) は維持. **影響**: routine-card.tsx 1 ファイル (JSX: `.routine-card__main` ラッパ撤去 + `.routine-card__header` div 新設で name + PriorityStars を囲う / 曜日 + 削除は基底直下に位置取り) + routine-card.css (`.routine-card` の flex-direction 変更 / `.routine-card__main` セレクタ撤去 / `.routine-card__header` 新設 / `.routine-card__input` フォント関連 / `.routine-card--form` の override 範囲再確認) + 既存 `routine-card-component.test.tsx` (BL-061) + `routine-card-edit-fields.test.tsx` (BL-068) + `routine-card-edit-priority.test.tsx` (BL-069) + `inline-edit-all-cards.test.tsx` (BL-070) の DOM 階層 assert があれば追従. E2E は role + accessibleName ベースのため無修正で通る想定. **完了の目安**: (a) `<RoutineCard>` の name input と PriorityStars が同一の `.routine-card__header` 親要素直下に並ぶ, (b) name input の computed font-size が `--font-size-h2` (= 20px) と一致, (c) PriorityStars が header 段の右端に視覚配置される (= `justify-content: space-between` で実現), (d) `<RoutineFormCard>` の見た目が現状と変わらない, (e) blur 書き戻し (BL-070) / 即時 PATCH (BL-068/069) / 削除の動作が回帰しない, (f) a11y violations 0 件維持, (g) 既存単体 / E2E green |
| BL-072 | RoutineFormCard レイアウト刷新 (RoutineCard と同じ 4 段構造に揃える) | P2 | Done | NFR-010 / BL-061, BL-068, BL-069, BL-071 依存 / 想定 `features/routine-form-card-header-layout/`. **背景**: BL-071 (`routine-card-header-layout` / commit `ff82142`) で `<RoutineCard>` (表示カード) を 3 段構造 (`.routine-card__header` [name input 左 + PriorityStars 右] / `.routine-card__day-checkboxes` / `.routine-card__actions`) に刷新したが, `<RoutineFormCard>` (起票カード) は同 BL の非ゴール (BL-071 REQ-7) として「現状の 2 段構造 (`.routine-card__form-row--name` [input + 追加 button] / `.routine-card__form-row--options` [曜日 + PriorityStars 右]) を維持」とされ手付かず. 結果として表示カードでは PriorityStars が右上 (header 段右) なのに起票カードでは PriorityStars が下段右に残り, user から「起票ルーティンの優先度の位置が変わってないよ」と齟齬指摘. **本 BL の方針**: `<RoutineFormCard>` を表示カードと同じイディオムの **4 段構造** (header [左空 + PriorityStars 右] / title 相当 [name input] / day-checkboxes [曜日 7 件] / actions [「追加」 submit button]) に組み替えて視覚言語を統一する. header 段左を「空」「placeholder 要素」「別要素 (例: 「追加」 button)」のどれにするか, name 段に何か併置するか, 「追加」 button の最終配置は plan の D-章で確定 (= 起票カードには表示カードの project chip 相当が無いため header 左の埋め物に選択肢がある). **スコープ**: (a) `web/src/ui/routine-card/routine-form-card.tsx` の JSX 再構築. (b) `web/src/ui/routine-card/routine-card.css` への CSS 追加・削除 (起票カード専用セレクタ `.routine-card__form-row` / `.routine-card__form-row--name` / `.routine-card__form-row--options` の取扱い + 起票カードの header / actions セレクタ設計 = `.routine-card__header` を表示カードと共用するか, `.routine-card--form .routine-card__header` で override するかは D-章で確定). (c) `RoutineFormCardProps` の API は無改修 (= name / onNameChange / daysOfWeek / onToggleDay / defaultPriority / onDefaultPriorityChange / onSubmit / inputId? / formAriaLabel? の 9 prop は維持). (d) `routines-view.tsx` の呼び出し側は無改修. **スコープ境界**: 表示カード `<RoutineCard>` には touch しない (= BL-071 の成果を維持). TaskCard / TaskFormCard / ProjectCard / ProjectFormCard / RoutinesView / WebRoutineRepository / API / domain / 共通 button (BL-067) / tokens.css には触れない. shadow / hover / transition / animation の追加禁止 (NFR-NO-SHADOW / NFR-NO-HOVER-TRANSITION). a11y (visually-hidden `ルーティン名` label + `htmlFor=routine-name` / PriorityStars `groupLabel="優先度"` + `idPrefix="routine-create"` / form `aria-label="ルーティン作成フォーム"` / 曜日 `role="group" aria-label="曜日"`) は全て維持. id 衝突回避 (表示カード `routine-name-{id}` / 起票カード `routine-name` / 表示 PriorityStars `routine-{id}` / 起票 PriorityStars `routine-create`) も維持. **影響**: routine-form-card.tsx (JSX 再構築) + routine-card.css (起票カード関連セレクタ追加・撤去) + 既存テスト追従 (`routine-card-component.test.tsx` BL-061 / `routine-card-edit-fields.test.tsx` BL-068 / `routine-card-header-layout.test.tsx` BL-071 AC-10 / AC-11 / `inline-edit-all-cards.test.tsx` BL-070 で起票カードの DOM 構造 assert があれば追従). 新規 `routine-form-card-header-layout.test.tsx`. E2E は role + accessibleName ベースのため無修正で通る想定 (`e2e/routines.spec.ts` 等). **完了の目安**: (a) `<RoutineFormCard>` の DOM が 4 段 (header / title 相当 / day-checkboxes / actions) の順で並ぶ, (b) header 段右端に PriorityStars が `space-between` 等で配置される, (c) name input の computed font-size が `--font-size-h2` (= 20px) と一致 (= 表示カードと統一), (d) 既存挙動 (onSubmit で routine 作成, controlled input, 曜日 7 checkbox の toggle, 優先度の変更) が回帰しない, (e) 表示カード `<RoutineCard>` には副作用なし, (f) a11y violations 0 件維持, (g) 既存単体 / E2E green. **未決**: U-1 (header 左の埋め物 = 空 / placeholder / 「追加」 button のうちどれを採用するか / 第一候補は「空」), U-2 (`.routine-card__form-row` 系セレクタを撤去するか維持するか / 第一候補は撤去), U-3 (「追加」 button を独立 actions 段に置くか name 段と同行にするか / 第一候補は actions 段独立), U-4 (起票 `.routine-card__header` を表示と共用するか `.routine-card--form` modifier で override するか / 第一候補は共用 = D-章で確定) |
| BL-073 | RoutineCard 表示カードのレイアウト刷新 (RoutineFormCard と同じ 4 段構造に揃える) | P2 | Done | NFR-010 / BL-061, BL-068, BL-069, BL-070, BL-071, BL-072 依存 / 想定 `features/routine-card-align-with-form/`. **背景**: BL-072 (`routine-form-card-header-layout` / commit `44f06a2` / main マージ済) で `<RoutineFormCard>` (起票カード) を 4 段構造 (`.routine-card__header` [左空 + PriorityStars 右] / `.routine-card__title` [name input] / `.routine-card__day-checkboxes` / `.routine-card__actions` [「追加」 button]) に再編した. 一方 `<RoutineCard>` (表示カード) は BL-071 で導入した 3 段構造 (`.routine-card__header` [name input 左 + PriorityStars 右] / `.routine-card__day-checkboxes` / `.routine-card__actions` [削除 button]) のまま残った. 結果 user 評価で「起票では name が 2 段目 (title 段) にあるのに表示では 1 段目 (header 段) にあって段位置が違って見える」と齟齬が指摘された. BL-071 当時は「TaskCard と同じ構造」を目指して header に name を置いたが, TaskCard は header の左に project chip が入る前提で chip があれば name は title 段に下がる. RoutineCard は chip を持たないため header 左が常に空 + name 段が起票より 1 段上にずれる結果になった. **方針**: 「カード系統間 (Task / Routine) の構造模倣」より「カード系統内 (Routine) の起票/表示の一貫性」を優先し, 表示カード `<RoutineCard>` も起票カードと同じ 4 段構造 (header [左空 + PriorityStars 右] / title [name input] / day-checkboxes / actions [削除 button]) に揃える. これにより表示カードの name input が 2 段目に下がり, 起票カードと段位置が完全一致する. **スコープ**: (a) `web/src/ui/routine-card/routine-card.tsx` の JSX 再構築 (header から name input + visually-hidden label を撤去, 新規 `.routine-card__title` 段に内包 / header には PriorityStars 単独を残す). (b) `web/src/ui/routine-card/routine-card.css` の調整 (`.routine-card__title` セレクタは BL-072 で新設済みのため表示カードでも流用 = 新規宣言なし. `.routine-card__header` 内 PriorityStars 単独で右端固定をどう実現するかは plan D-章で確定. 候補: D-a 起票カードの `.routine-card--form .routine-card__header { justify-content: flex-end }` override を `.routine-card__header` 基底に格上げ. D-b PriorityStars wrap に `margin-left: auto`. D-c 表示カードにも `.routine-card--display` 等 modifier を新設して別 override). (c) `RoutineCardProps` の API は無改修 (= name / onNameBlur / onDaysOfWeekChange / onDefaultPriorityChange / onDelete / as? の 6 prop は維持). (d) routines-view.tsx 呼び出し側は無改修. **スコープ境界**: 起票カード `<RoutineFormCard>` には touch しない (= BL-072 の成果を維持). TaskCard / TaskFormCard / ProjectCard / ProjectFormCard / WebRoutineRepository / API / domain / 共通 button (BL-067) / tokens.css には触れない. shadow / hover / transition / animation の追加禁止 (NFR-NO-SHADOW / NFR-NO-HOVER-TRANSITION). a11y (visually-hidden `ルーティン名` label + `htmlFor="routine-name-{id}"` / PriorityStars `groupLabel="${routine.name} の優先度"` + `idPrefix="routine-{id}"`) は全て維持. id 衝突回避 (表示 input id `routine-name-{id}` / 起票 `routine-name` / 表示 PriorityStars `routine-{id}` / 起票 `routine-create`) も維持. BL-070 D-001 (= 同値 blur 短絡は親側) / D-002 (= 空文字 blur 静かな元値復元 / uncontrolled input + key 再マウント) / BL-068 D-003 (即時 PATCH) / 削除の挙動は回帰させない. **影響**: routine-card.tsx (JSX 再構築) + routine-card.css (PriorityStars 単独右端固定の機構導入 / plan D-章で確定) + 既存テスト追従 (`routine-card-component.test.tsx` BL-061 / `routine-card-edit-fields.test.tsx` BL-068 / `routine-card-edit-priority.test.tsx` BL-069 / `inline-edit-all-cards.test.tsx` BL-070 / `routine-card-header-layout.test.tsx` BL-071 / `routine-form-card-header-layout.test.tsx` BL-072 の 6 ファイルで表示カードの DOM 階層 assert があれば追従). 新規 `routine-card-align-with-form.test.tsx`. E2E は role + accessibleName ベースのため無修正で通る想定 (`e2e/routines.spec.ts` 等). **完了の目安**: (a) `<RoutineCard>` の DOM が 4 段 (header / title / day-checkboxes / actions) の順で並ぶ, (b) header 段は PriorityStars 単独を含み右端に固定される (= 起票カードと同イディオム), (c) name input が `.routine-card__title` 段に内包され computed font-size が 20px (= 起票カードと統一), (d) 起票カード `<RoutineFormCard>` の DOM 構造・見た目に副作用なし, (e) BL-070 (空文字 blur 元値復元 / 同値 blur 短絡) / BL-068 / BL-069 (即時 PATCH) / 削除の挙動が回帰しない, (f) a11y violations 0 件維持, (g) 既存単体 / E2E green. **未決**: U-1 (PriorityStars 単独右端固定の機構 = (a) `.routine-card__header` 基底を `justify-content: flex-end` に変える / (b) 表示側に modifier 新設して別 override / (c) `space-between` のまま PriorityStars wrap div + `margin-left: auto` / 第一候補は plan D-章で確定), U-2 (header 左に何も置かないため空 `<div>` 等の placeholder を入れるか / 第一候補は「何も置かない」). |
| BL-074 | アプリ内パスワードログインの導入 (Web の URL 知見アクセスを遮断) | P1 | Done | NFR-002 / `features/app-login/` 完了. パスワード照合は bcrypt ハッシュ (`app_password` テーブル) + opaque session token (`crypto.randomBytes(32).toString("hex")` を `sessions` テーブルに保存 / 30 日有効). `POST /api/v1/login` / `POST /api/v1/logout` / authMiddleware は session lookup. 共通 `LoginView` (`web/src/ui/login-view/`) + `auth-storage` 抽象 (Web: localStorage / Android: Capacitor Preferences) + `authedFetch` (401 で token 破棄 + `todica:auth-expired` Custom Event) + SettingsView 内ログアウト button. 旧 `AUTH_TOKEN` / `VITE_AUTH_TOKEN` は完全廃止. |
| BL-070 | 全カード (Task / Project / Routine) のインライン常時編集化 + 「変更/保存/キャンセル」 button 撤去 | P2 | Done | NFR-010 / BL-042, BL-059, BL-060, BL-061, BL-068, BL-069 依存 / 想定 `features/inline-edit-all-cards/`. user 要求「編集は常に可能にしたい. これはすべてのカードで共通. 変更ボタンは不要」. **採用方針**: 全 3 系統 (TaskCard / ProjectCard / RoutineCard) の「編集モード」概念 (`isEditing` / `editing*` / 「変更」「保存」「キャンセル」 button) を撤去し, 表示時に input / 曜日 checkbox / PriorityStars を常時描画して直接編集可能にする. 保存タイミングは **案 e** (= 状態系 [曜日 / 優先度] は click ごとに即時 PATCH / テキスト [name] は blur PATCH). 同値 blur / 空文字 blur は親 view 側で短絡し PATCH を送らない (= D-001 / D-002: 空文字は静かな元値復元). 412 は既存 ConflictDialog 経路を流用 (BL-031 / BL-033 / D-003). **スコープ**: (1) TaskCard: `task-card__title` 内の `<span>{task.name}</span>` を `<input value={task.name} onBlur={...} aria-label={`${task.name} の名前`}>` に置換. `TaskCardProps` に `onNameBlur: (next: string) => void` を必須追加. PriorityStars (BL-040) / actions は無改修. today / tomorrow / focus の 3 view で `<TaskCard>` に `onNameBlur` を渡す. (2) ProjectCard: 6 prop (`isEditing` / `editingName` / `onEditingNameChange` / `onStartEdit` / `onCancelEdit` / `onSaveEdit`) を撤去, `onNameBlur` を追加. 表示 DOM は `<label visually-hidden>` + `<input id="project-name-{id}">` + 「削除」 button のみ. `.project-card--editing` / `.project-card__form-inline` / `.project-card__actions__edit` / `.project-card__name` セレクタを撤去. (3) RoutineCard: 10 prop (`isEditing` / `editingName` / `onEditingNameChange` / `editingDaysOfWeek` / `onEditingDaysOfWeekChange` / `editingDefaultPriority` / `onEditingDefaultPriorityChange` / `onStartEdit` / `onCancelEdit` / `onSaveEdit`) を撤去, `onNameBlur` / `onDaysOfWeekChange` / `onDefaultPriorityChange` を追加. 表示 DOM は name input + 曜日 7 checkbox + PriorityStars (`idPrefix={`routine-${routine.id}`}`) + 「削除」 button. `.routine-card--editing` / `.routine-card__form-inline` / `.routine-card__actions__edit` / `.routine-card__days-label` / `.routine-card__name` を撤去. (4) projects-view: `editingId` / `editingName` state + `openEdit` / `cancelEdit` / `handleSaveEdit` handler を撤去, `handleNameBlur` を新設. (5) routines-view: 4 state + 3 handler を撤去, `handleNameBlur` / `handleDaysOfWeekChange` / `handleDefaultPriorityChange` を新設. 曜日 0 件 silent return 維持. **スコープ境界**: TaskFormCard / ProjectFormCard / RoutineFormCard (= 起票カード) は無改修 / 「追加」 button は維持. domain / server / Repository / Mutation interface は無改修. ConflictDialog / notifyError 経路は維持. focus-view の actions 2 ボタン制約は維持. tokens.css 無改修. shadow / hover / transition / animation 追加なし. **PR 方針**: 1 PR でまとめて提出 (採用案 α). 各カードの API 変更と親 view の handler 再設計は同時に行う必要があり分割すると中間状態でテストが green にならない. branch: `feature/inline-edit-all-cards`. **D 章**: D-001 (同値 blur 短絡は親側) / D-002 (空文字 blur = 元値復元 / 静かな扱い) / D-003 (412 = ConflictDialog 流用) / D-004 (テスト: 新規 + card 系 6 / view 系 2 / E2E 4 ファイルの追従) / D-005 (BL-042 spec のみ注釈追記 / 他は履歴を git / backlog で追跡) / D-006 (起票カード無改修) / D-007 (view handler 再設計の詳細) / D-008 (BL-042「代替経路は別 BL」の本実装 = カードタップダイアログ案ではなく常時 input 採用) / D-009 (a11y: TaskCard は aria-label / Project & Routine は entity id suffix の visually-hidden label) / D-010 (一覧で input が並ぶ UX = 何もしない / 別 BL 候補) / D-011 (1 PR / 採用案 α) / D-012 (案 e / 状態系即時 + テキスト blur) / D-013 (進行順 = カード 3 種並行 → views 順次). **影響**: TaskCard / ProjectCard / RoutineCard 3 ファイル + 親 view 5 ファイル (today / tomorrow / focus / projects / routines) + project-card.css / routine-card.css 各 5 セレクタ撤去 + 既存テスト追従 (task-card-component / task-card-hotfix / task-card-zone-layout / project-card-component / routine-card-component / routine-card-edit-fields / routine-card-edit-priority / projects-view / routines-view の 9 ファイル) + E2E 4 ファイル追従 (conflict-handling / projects / routines / tasks) + BL-042 spec への注釈 1 行追記 (2 か所). 新規 `inline-edit-all-cards.test.tsx` (AC-1 〜 AC-21 / AC-23). 新規 component / 新規 CSS / tokens.css / Repository / Mutation interface / 共通 button (BL-067) 改修なし. **完了の目安**: (a) 全 3 系統で `isEditing` / `editing*` / 「変更」「保存」「キャンセル」 が DOM / props から完全撤去, (b) 全 3 系統で name input + (Routine のみ) 曜日 + PriorityStars が常時表示, (c) blur で名前 PATCH / click で曜日・優先度 PATCH, (d) 空文字 / 同値 blur で PATCH なし, (e) 412 で ConflictDialog 起動, (f) 起票カード 3 種が無改修, (g) 既存単体 / E2E / a11y violations 0 件維持. **未決**: U-1 (input 並びの UX 許容 / 別 BL 候補) / U-2 (空文字 blur 時の notifyError 出すか = 第一候補は出さない) / U-3 (focus-view の autoFocus 非設定) / U-4 (即時 PATCH 並行時の version 競合 = 1 click ごと `await mutateAsync` で逐次化) |

| BL-075 | SettingsView の旧 authToken 入力 UI を削除 (BL-074 dead path 整理) | P3 | Done | `features/settings-view-dead-path-cleanup/` 完了. `SettingsView` から `serverUrl` / `authToken` / `onSaveServer` props と関連の useState / 入力欄 / 保存 button + dead path 検証テスト 2 件を撤去. SettingsView の責務はリセット時刻設定 + ログアウト + サーバ/ローカル切替 + パスワード変更に絞られている. |
| BL-077 | E2E (Playwright) に LoginView シナリオを追加 | P2 | Done | NFR-002 / `features/e2e-login-spec/` 完了. `e2e/login.spec.ts` で (1) 未認証アクセス → LoginView 表示, (2) 誤ったパスワード → 401 + エラーメッセージ + LoginView 留まる, (3) 正しいパスワード → 今日ビューへ遷移, (4) SettingsView ログアウト → LoginView 戻り, (5) token 保持時の本体ビュー直接表示 の往復シナリオを Playwright で検証. |
| BL-080 | env 廃止 + ブラウザからの初期パスワード設定を必須化 | P1 | Done | NFR-002 / NFR-013 / BL-074 / BL-079 依存 / 想定 `features/initial-password-setup/`. **背景**: `APP_PASSWORD_HASH` env による初期 seed が SSH + bcrypt CLI を強制し, VPS デプロイ / Play Store 配布の体験を悪化させている. パスワードを env で管理する意味は BL-079 の DB 永続化導入で失われており, env の存在自体が陳腐化している. **方針**: `APP_PASSWORD_HASH` env と password-seed.ts を完全削除. サーバは env なしで起動可能とし, DB の `app_password` が空のときは初期設定モードに入る. クライアントは起動時に `GET /api/v1/auth-state` で `initialized` を確認し, `false` なら InitialSetupView (新 PW + 確認 PW の 2 入力, 現在 PW 欄なし) を表示する. 送信成功で自動ログイン → `/today` 遷移. `POST /api/v1/password` は「DB が空のときは認証不要 + currentPassword 不要」に拡張する. **スコープ**: (1) `APP_PASSWORD_HASH` env / password-seed.ts / .env.example の対応行を削除. (2) `GET /api/v1/auth-state` (認証不要) を新設し `{ initialized: boolean }` を返す. (3) `POST /api/v1/password` を初期設定 (DB 空) と変更 (DB 値あり) の 2 モードに分岐. (4) `POST /api/v1/login` は DB が空のときは 412 `INITIAL_SETUP_REQUIRED` を返す. (5) `web/src/auth/auth-state-client.ts` 新規 + `password-client.ts` に `setupInitialPassword(baseUrl, newPassword)` を追加. (6) `web/src/ui/initial-setup-view/` 新規 (LoginView と類似だが現在 PW 欄なし + 確認 PW あり). (7) `web/src/main.tsx` の起動分岐に `auth-state` 確認を組み込み, `initialized: false` → InitialSetupView. (8) docs (`.env.example` / quick-start / deploy-guide / setup/server / faq) 全件更新: env 完全廃止 / 初回起動はデプロイ者がただちにブラウザにアクセスして初期設定 / セキュリティ注意 (deploy 後の access window). **スコープ境界**: パスワードリセット (メール経由) / 強度ポリシー / 多要素認証 / マルチユーザは範囲外. **完了の目安**: (a) `APP_PASSWORD_HASH` env を completely 廃止 (`grep` で 0 hit), (b) サーバが env なしで正常起動する, (c) DB 空のときブラウザで `/` を開くと InitialSetupView, (d) 設定完了で `/today` に自動遷移してそのままタスク作成できる, (e) DB に password があるときは従来通り LoginView, (f) 既存 1726 件 + 新規分すべて green, (g) typecheck / lint 0 エラー. **未決**: なし. |
| BL-079 | ブラウザからのパスワード変更 UI (DB persistence + SettingsView 内フォーム) | P2 | Done | NFR-002 / NFR-013 / BL-074 依存 / 想定 `features/password-change/`. **背景**: 現状はパスワードハッシュが `APP_PASSWORD_HASH` env で固定されており, 変更には SSH + `.env` 編集 + サーバ再起動が必要. 個人運用とはいえブラウザから変更できないのは UX 上の負債. **方針**: DB の `app_password` テーブル (単一行) に bcrypt ハッシュを保持し, サーバ起動時に DB が空なら `APP_PASSWORD_HASH` env で seed する. 以降の認証も DB の値を参照する. SettingsView に「パスワード変更」セクションを追加し, 現在パスワード + 新パスワード + 新パスワード確認の 3 入力で `POST /api/v1/password` を叩く. 成功時は notify + 他端末の sessions を一括 DELETE して LoginView に遷移する. **スコープ**: (1) drizzle マイグレーション `app_password` テーブル (id PK = "current", password_hash, updated_at). (2) `server/src/data/password-repository.ts` + Drizzle 実装. (3) `server/src/main.ts` で DB が空なら env で seed, あれば DB を採用 / authMiddleware と /login が DB hash を参照. (4) `POST /api/v1/password` ハンドラ: 現在パスワード照合 → 新パスワードの hashSync(12) → DB 更新 → sessions DELETE all → 200. (5) `web/src/ui/settings-view/settings-view.tsx` に「パスワード変更」セクション (3 input + submit). 401 / 400 のエラー表示. 成功時は LoginView に遷移. (6) `web/src/auth/password-client.ts` 新規. (7) env / docs (deploy-guide.md / quick-start.md / setup/server.md / faq.md) を更新. (8) 新規 spec / plan / tasks ファイル + テスト全件. **スコープ境界**: パスワードリセット (メール経由) / パスワード強度ポリシー / 多要素認証 / マルチユーザ管理は範囲外. **完了の目安**: (a) ブラウザの SettingsView から現在 PW + 新 PW + 確認 PW を入力して保存できる, (b) 保存後は LoginView に強制遷移して新 PW でログイン可能, (c) 他端末のセッションも一斉に無効化される, (d) サーバ初回起動は `APP_PASSWORD_HASH` env で seed され, 以降は DB が真実の source, (e) 既存 1678 件 + 新規分すべて green, (f) typecheck / lint 0 エラー. **未決**: なし. |
| BL-078 | `authedFetch` と `CapacitorAuthStorage` の単体テストを追加 | P2 | Done | NFR-002 / `features/auth-storage-tests/` 完了. `web/src/auth/authed-fetch.test.ts` で 401 捕捉時の `clearToken` 呼び出し + Custom Event dispatch + 非 401 エラーの素通しを検証. `web/src/auth/capacitor-auth-storage.test.ts` で `vi.mock("@capacitor/preferences")` を介して `Preferences.get` / `set` / `remove` が `todica.auth.token` key で叩かれることを検証. |
| BL-076 | 5 本 HTTP Repository を `authedFetch` 経由に統一 | P1 | Done | NFR-002 / `features/authed-fetch-repositories/` 完了. `HttpTaskRepository` / `HttpSettingsRepository` / `HttpProjectRepository` / `HttpRoutineRepository` / `HttpTrashRepository` の全 5 本が `authedFetch` 経由で fetch する. いずれの Repository でも 401 で token が破棄され `todica:auth-expired` Custom Event が dispatch されるため, 認証期限切れで全 view が同じ挙動 (LoginView へ遷移) になる. 旧 `authToken` constructor 引数は全 5 本から撤去済. |
| BL-081 | README / CONTRIBUTING / quick-start のオンボーディング不整合を解消 | P1 | Done | `features/onboarding-doc-repair/` 完了. README / CONTRIBUTING / docs/user/quick-start.md / docs/user/deploy-guide.md の 4 文書で Node 要件文言を「Node.js 24.x（手元の動作確認バージョン。他バージョンは未検証）」に統一. README の「サーバの起動」を dev 起動 (`npm run dev -w server`) に変更し, 本番運用 (`npm start -w server`) は事前ビルド (`npm run build -w domain && npm run build -w server`) が必要であることと deploy-guide.md へのリンクを明記. |
| BL-082 | backlog.md と完了済 feature の tasks.md を実装状態に追従させる | P2 | Done | `features/backlog-status-sync/` 完了. backlog.md の status 列を実コードに揃え, BL-074 / 075 / 076 / 077 / 078 / 091 を Done に. Done 済 feature の tasks.md (app-login / settings-view-dead-path-cleanup / authed-fetch-repositories / e2e-login-spec / auth-storage-tests / initial-password-setup / password-change / reset-time-rework) のチェックボックスを `[x]` に揃え, 過去の Todo 残骸を消化. |
| BL-083 | 完了済 feature ドキュメントから旧認証経路 (AUTH_TOKEN / VITE_AUTH_TOKEN / APP_PASSWORD_HASH) 参照を除去 | P2 | Done | `features/auth-doc-cleanup/` 完了. android-server-mode / e2e-login-spec / authed-fetch-repositories / settings-view-dead-path-cleanup / task-crud / web-client-foundation の plan / spec を現行 auth フロー (sessions + opaque token + /api/v1/login + /password + /auth-state) に揃えて timeless 化. server-foundation / password-change / oss-release-prep の env 縮退中間状態は tasks.md 末尾「## 経緯」節に隔離. `__tests__/docs/no-legacy-auth-refs.test.ts` で 4 ディレクトリ配下の旧トークン参照が経緯節以外で 0 件であることを検証. |
| BL-084 | `web/src/main.tsx` の `as any` 4 件を型付きで解消 + 死んだ eslint コメント撤去 | P2 | Done | `features/main-tsx-any-cleanup/` 完了. `web/src/repositories/local-db.ts` に `LocalDb` interface を新設し `getDb(): Promise<LocalDb>` に強化. 6 Local 系ファイル (local-task / settings / trash / project / routine repository + local-reset-usecase) の個別 `DBConnection` interface を撤去して `LocalDb` を import. `main.tsx` の 4 箇所の `const anyDb = db as any;` + `eslint-disable-next-line` を撤去し `db` を直接渡す. lint warnings 4 → 0. |
| BL-085 | `web/src/main.tsx` と `server/src/app.ts` の責務分割 | P3 | Done | `features/entrypoint-decomposition/` 完了. `server/src/app.ts` (1337 行) を 101 行に縮退し createApp をルータ配線のみに. リソース別 10 router (`server/src/routers/`: tasks / today / focus / counter / settings / routines / projects / trash / reset / auth) + `server/src/middleware.ts` (共通ヘルパ). `web/src/main.tsx` (555 行) を 27 行に縮退し ReactDOM.createRoot + render のみに. `bootstrap.ts` (AuthState + repo 構築) / `app.tsx` (ルートコンポーネント) / `routes.tsx` (Routes + ProtectedRoute) に責務分離. 全分割ファイル 400 行以下. `__tests__/structure/entrypoint-decomposition.test.ts` で構造制約を検証. |
| BL-086 | サーバ CORS の origin 許可リスト化 | P2 | Done | `features/cors-origin-allowlist/` 完了. `cors({ origin: "*" })` を origin callback に変更し `AppDeps.allowedOrigins` で受け取る. `parseAllowedOrigins(envValue)` を別 export (カンマ区切り → trim → 空要素除外). default = `["http://localhost:5173", "capacitor://localhost"]`. `server/src/main.ts` で `ALLOWED_ORIGINS` env を読んで渡す (`DATABASE_PATH` / `PORT` と同じ規約). 許可外 origin の preflight は CORS ヘッダ無しで拒否. `.env.example` / `docs/developer/setup/server.md` / `docs/user/deploy-guide.md` に説明追加. |
| BL-087 | `server/src/data/` (interface) と `server/src/infra/persistence/drizzle/` (実装) のファイル名衝突を解消 | P3 | Done | `features/repo-impl-rename/` 完了. `server/src/infra/persistence/drizzle/` 内 9 ファイル (counter / focus / idempotency-store / password / project / routine / session / settings / task) を `drizzle-` 接頭辞付きに rename. interface ファイル (`server/src/data/*-repository.ts`) は無改修. doc コメントで本実装の場所を示す箇所も `drizzle-` 接頭辞付きパスに追従. |
| BL-088 | `setup/server.md` / `quick-start.md` の `VITE_API_BASE_URL` 説明を `.env.example` の dev / 本番使い分けに整合 | P3 | Done | `features/api-base-url-doc-align/` 完了. `setup/server.md` の env 表直下に `VITE_API_BASE_URL` の dev (`http://localhost:3000`) / 本番 (空文字 = 相対パス) の用途差を注記. `quick-start.md` A-2 を 2 段に分け dev 想定 + Web の build 時埋め込み + サーバ dev 起動の env ランタイム読みを明示. `__tests__/docs/api-base-url-doc-align.test.ts` で grep ベース検証. |
| BL-089 | `package.json` に `clean` / `clean:dist` script を追加 | P3 | Done | `features/repo-clean-script/` 完了. `clean`: `rimraf -g test-results web/dev-dist .e2e-data "**/*.tsbuildinfo"` (rimraf v6 で glob を効かせる `-g` 必須). `clean:dist`: `rimraf domain/dist server/dist web/dist && npm run build -w domain`. `__tests__/structure/repo-clean-script.test.ts` で script 存在と sentinel ファイル削除の実動作を検証. |
| BL-090 | `vitest run` の skipped 198 件を棚卸し | P2 | Done | `features/skipped-tests-audit/` 完了. UI 再設計で superseded された skip テスト群を 7 ファイルから削除 (うち 2 ファイルは retired suite 全体). 結果: 1863 passed | 198 skipped → 1870 passed | 0 skipped. `__tests__/structure/no-obsolete-skips.test.ts` で対象 5 ファイルの skip 行 0 件を assert. `docs/developer/quality/test-catalog.md` に skip 状況節を追加. passed テスト側に残る BL-XXX 履歴表現の整理は BL-095 で対応. |
| BL-091 | リセット時刻 (旧称: 境界時刻) のサーバ TZ 解釈 + UI 整理 | P1 | Done | `features/reset-time-rework/` 完了. (1) `server/src/use-cases/daily-reset.ts` の `calcTodayBoundaryAt` を `(nowIso, dayBoundaryTime, timeZone) => string` の 3 引数純関数化. `maybeRunDailyReset` は `getServerTimeZone()` (= `process.env.TZ ?? Intl.DateTimeFormat().resolvedOptions().timeZone`) を解決して渡す. `<wall-clock>` 比較で DST 非考慮. (2) SettingsView の `<label>` を「リセット時刻」に変更し input.value のみで現在値を表現. `.settings-view__current` ブロックは撤去. (3) `docs/user/faq.md` / 既存 `daily-reset/spec.md` / `settings-day-boundary/spec.md` の TZ 関連記述を本 feature 参照に追従. (4) Android Local Reset Usecase も端末 TZ (`Intl.DateTimeFormat().resolvedOptions().timeZone`) で同じ判定にする. OpenAPI / DB schema / 関数名 / input id は無改修. |
| BL-092 | SW アップデート確認 UI を画面中央のダイアログ化 | P2 | Done | `features/sw-update-dialog/` 完了. 旧 `PwaUpdateBanner` を `<dialog>` + `showModal()` ベースの画面中央モーダル `SwUpdateDialog` に置換. `aria-modal="true"` + `aria-label="アップデート"` + native role=dialog. 2 ボタン: 「再読み込み」 (primary / `SKIP_WAITING` + `window.location.reload()`) と「あとで」 (ghost / `close()`). Escape / overlay click でも dismiss. `web/__tests__/sw-update-dialog.test.tsx` で 8 シナリオ検証. |
| BL-093 | 画面右上にアイコンのみの更新ボタンを配置 | P2 | Done | `features/app-shell-reload-button/` 完了. AppShell 右上にアイコンのみ `↻` の button を配置. `aria-label="アップデートを確認して再読み込み"`. クリック handler: waiting SW があれば `SKIP_WAITING` 送信, 無ければ `registration.update()`, いずれの場合も `window.location.reload()`. SW 未対応環境では reload のみ. `web/__tests__/app-shell-reload-button.test.tsx` で全 3 経路を mock 切替で検証. |
| BL-094 | SettingsView の構造・文言整理 (4 件まとめ) | P2 | Done | `features/settings-view-cleanup/` 完了. (1) ログアウト二重箱解消: `<section>` を `.settings-view__logout` に切替 (border / padding なし). (2) パスワード変更フォーム縦並び: 3 ブロックを `.settings-view__password-field` (flex column) + label display: block. (3) submit button 文言「変更」統一 (旧: 保存 / 更新 / 変更する). (4) リセット時刻ラベル太字 + 2 段構成: `.settings-view__label` (font-weight: bold) と `.settings-view__field-row` 内に input + 「変更」 button が横並び. `web/__tests__/settings-view-cleanup.test.tsx` で 4 FR を直接 assert. |
| BL-095 | 完了済 UI feature テストファイルから BL-XXX 履歴表現を timeless 化 | P3 | Done | `features/test-historical-phrasing-cleanup/` 完了. routine-card / project-card 系の単体テスト 6 ファイル (routine-card-component / project-card-component / routine-card-header-layout / routine-form-card-header-layout / routine-card-align-with-form / routine-card-edit-priority 等の現役分) の describe / it タイトル + コメントから `BL-0[0-9]+` 履歴表現を削除または現行設計を表す中立表現に置換. 各 test の assertion 意図は無改修. |
| BL-096 | Service Worker 登録コードを `main.tsx` に追加 (PWA 機能の復活) | P1 | Todo | 想定 `features/sw-register-fix/`. **背景**: `web/vite.config.ts` で `VitePWA({ registerType: "prompt", strategies: "injectManifest" })` を設定しているが, `import { registerSW } from "virtual:pwa-register"` を呼び出すコードが `web/src/main.tsx` および全コードベースに存在しない. 結果として Service Worker が一切登録されず, precaching (PWA-002) / Background Sync (WQ-005) / SwUpdateDialog (BL-092) の waiting 検知 / 右上更新ボタン (BL-093) の `registration.getRegistration()` の戻り値, いずれも実機で機能しない. **方針**: (a) `main.tsx` に `import { registerSW } from "virtual:pwa-register"` を追加し `registerSW({ onNeedRefresh, onOfflineReady, onRegistered })` を 1 度呼び出す. `onNeedRefresh` / `onRegistered` のコールバックは現状 SwUpdateDialog 側で waiting を polling する設計と整合させる (重複検知を避ける). (b) dev サーバでは vite-plugin-pwa の dev SW (`/dev-sw.js?dev-sw`) が読まれる経路を確認. (c) `import.meta.env.MODE` を見て dev のみ登録をスキップする等の安全策は必要に応じて入れる. **スコープ境界**: SW 本体 (`web/src/sw/service-worker.ts`) のロジックは無改修. SwUpdateDialog / AppShell reload button の改修も不要 (これらは SW が動けば機能する). **完了の目安**: (a) prod build (`npm run build -w web`) 後 `dist/sw.js` が生成され, ブラウザで `/index.html` を開いたとき DevTools の Application タブで Service Worker が registered + activated になる, (b) 既存 1894 テストが green を維持, (c) typecheck / lint 0, (d) `web/__tests__/main-registers-sw.test.tsx` または structure test で `registerSW` import が main.tsx に存在することを assert. **未決**: U-1 (`registerSW({ onNeedRefresh })` の callback と SwUpdateDialog の polling 経路をどう統合するか / 第一候補は SwUpdateDialog 側 polling を維持し registerSW の callback は no-op で登録のみ). |
| BL-097 | offline-queue の認証ヘッダを auth-storage 経由に修正 + `HasBaseUrlAndToken` dead path 撤去 | P1 | Todo | 想定 `features/offline-queue-auth-fix/`. **背景**: BL-074 (auth-storage 導入) / BL-076 (5 Repository を `authedFetch` 統一) 後, UI 層 6 view (`today-view` / `tomorrow-view` / `projects-view` / `routines-view` / `trash-view` / `focus-view`) + `project-create-dialog` で `const authToken = repo.authToken ?? "";` を `Authorization: Bearer ${authToken}` ヘッダに乗せて `offline-queue.enqueue()` している. しかし `HttpTaskRepository` 等は BL-076 で `authToken` constructor 引数を撤去済のため `repo.authToken` は常に `undefined` → 空文字フォールバック. 結果として offline 中に書き込んだ全 mutation が `Authorization: Bearer ` (空 token) で enqueue され, `offline-queue.ts flush()` も `entry.headers` をそのまま `fetch()` に渡すので, online 復帰時にサーバが 401 で全件却下 → MAX_RETRY=5 で drop されて消える. = **オフライン書込キュー (BL-018) が事実上機能していない**. **方針**: (a) UI 層から `HasBaseUrlAndToken` interface 7 ファイル分の重複定義を撤去し, `repo.baseUrl` だけは存続させる (offline-queue の URL 組み立てに必要). `repo.authToken` 参照は全削除. enqueue 時の `Authorization` ヘッダもセットしない. (b) `offline-queue.ts flush()` を `authedFetch` 経由 (auth-storage から都度 token を読む) に変更し, entry には Authorization を含めない. (c) 既存 offline-queue テスト (`offline-queue.test.ts` 等) を新仕様に追従. **スコープ境界**: server 側 API / SW 本体 / Repository constructor 引数 / domain 層 / auth-storage 実装は無改修. **完了の目安**: (a) 6 view + 1 dialog から `HasBaseUrlAndToken` / `repo.authToken` 参照が消える, (b) offline 中に enqueue したリクエストが online 復帰時に `authedFetch` で auth-storage 由来のフレッシュ token で送信され 200 を返す, (c) 既存テスト + 新規テストで「flush 時に auth-storage の token が Authorization ヘッダに乗る」を検証, (d) typecheck / lint 0. **未決**: U-1 (entry に `Authorization` ヘッダを残したまま flush 側で上書きするか, 完全に削除するか / 第一候補は完全削除 = enqueue 時に Authorization を入れない), U-2 (auth-storage 利用不可環境 (Capacitor Preferences ロード失敗等) でのフォールバック挙動 / 第一候補は flush をスキップして次回 online 復帰時にリトライ). |
| BL-098 | major dependencies の更新 (React 19 / biome 2.x / drizzle 0.45 等) | P3 | Todo | 想定 `features/dep-major-updates/`. **背景**: `npm outdated` で major level の更新待ちが多数: React 18.3 → 19.2, @types/react 18.3 → 19.2, @vitejs/plugin-react 4.7 → 6.0, @biomejs/biome 1.9 → 2.5, drizzle-orm 0.38 → 0.45, drizzle-kit 0.30 → 0.31, @vitest/coverage-v8 2.1 → 4.1, better-sqlite3 11.10 → 12.10, @types/node 22.19 → 25.9 等. major 更新は API 互換性破れの可能性があり段階的に対応が必要. **方針**: 1 PR で全部を更新しない. パッケージ単位 (例: React 19, biome 2.x, drizzle 0.45, vitest 4.x) で別々の PR に分割する. React 19 は jsx-runtime / forwardRef 等の挙動変更があるため最重要. biome 2.x は lint ルールが変わる可能性があるため設定ファイルの追従が要る. drizzle-orm 0.45 は schema DSL / migration の breaking change を確認. **スコープ境界**: minor / patch 更新は本 BL とは別 (自動更新 or chore commit で随時). **完了の目安**: (a) パッケージごとの updated PR が個別に merge され, (b) 既存テスト全件 green / typecheck / lint 0, (c) prod build と Android Capacitor sync が成功. **未決**: U-1 (どのパッケージから優先的に更新するか / 第一候補は biome 2.x (lint 設定追従が最小スコープで完結) → React 19 → drizzle 0.45 → vitest 4 の順), U-2 (React 19 は @types/react 19 / @types/react-dom 19 / @vitejs/plugin-react 6 と同時更新の必要があるか確認 / 第一候補は同時更新). |
| BL-099 | SettingsView リセット時刻ラベルの文字サイズを `<h2>` と統一 (BL-094 フォローアップ) | P2 | Done | `features/settings-view-label-size/` 完了. `.settings-view__label` (リセット時刻) のフォントサイズが `<h2>` (パスワード変更 / モード切替) の `--font-size-h2` (20px) と異なりブラウザ既定 (~16px) のままで視覚階層に不整合があった. CSS で `.settings-view__label` に `font-size: var(--font-size-h2)` を追加し 20px に揃える. `web/__tests__/settings-view-cleanup.test.tsx` FR-4 の assertion を 太字 + h2 サイズ の 2 宣言検証に拡張. |
| BL-100 | SettingsView リセット時刻入力欄の文字サイズを `<h2>` と統一 (BL-099 フォローアップ) | P3 | Done | `features/settings-view-input-size/` 完了. `.settings-view__field-row input` (リセット時刻入力欄) のフォントサイズがブラウザ既定で, BL-099 でラベルを `--font-size-h2` (20px) に揃えた後も入力欄だけ小さく見える違和感が残った. CSS で `.settings-view__field-row input` に `font-size: var(--font-size-h2)` と `padding: var(--space-xs) var(--space-sm)` を追加して縦横ともに視覚的に大きくする. |
