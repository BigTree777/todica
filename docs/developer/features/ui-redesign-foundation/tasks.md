# タスク: UI 全面刷新の総合仕様 (デザイン基盤と差分カタログの確定)

> [`plan.md`](plan.md) を実行可能な単位に分解する. 本 BL は **foundation** であり, 実装作業はほぼ持たず, planning タスクが中心となる. 完了したらチェックを入れる.

## 仕様 / 設計確定

- [x] `docs/developer/features/ui-redesign-foundation/` ディレクトリの作成 (`_template/` から派生)
- [x] spec.md の起票 (背景 / ゴール / 非ゴール / UI モックアップの構造 / 要件 REQ-1〜REQ-8 / 差分カタログ / 受け入れ基準 / Open Questions)
- [x] plan.md の起票 (方針概要 / アーキテクチャ / 後続 BL 依存関係グラフ / 影響範囲 / 設計詳細 / 重要な決定 D-001〜D-010 / リスク / テスト方針)
- [x] tasks.md の起票 (本ファイル)
- [ ] auditor によるドラフトレビュー
  - [ ] 差分カタログから後続 BL の責務範囲が一意に読み取れる粒度であること
  - [ ] 後続 BL の依存関係グラフが完全 (BL-036 〜 BL-047 全 12 件が登場) であること
  - [ ] 既存 FR / NFR の不変条件 (API / データモデル / ドメイン値域変更なし) が spec / plan のどちらでも明示されていること
  - [ ] Open Questions が後続 BL のいずれかに割り当てられていること
- [ ] auditor 承認後, spec.md / plan.md のフロントメタの状態を「ドラフト」→「確定」に更新

## 後続 BL の独立着手可能性確認

> 各 BL の spec ドラフトを書き始められるかを本 spec / plan のみを参照してシミュレーションする.

- [ ] BL-036 (AppShell + 3 ルート骨格) の spec ドラフト着手可能性確認
- [ ] BL-037 (focus-view 切り出し) の spec ドラフト着手可能性確認 (BL-036 完了後)
- [ ] BL-038 (tomorrow-view 新規) の spec ドラフト着手可能性確認 (BL-036 完了後)
- [ ] BL-039 (起票フォーム期限セレクト削除) の spec ドラフト着手可能性確認 (BL-037 + BL-038 完了後)
- [ ] BL-040 (優先度 星 3 つ UI) の spec ドラフト着手可能性確認 (独立)
- [ ] BL-041 (プロジェクトトグル UI) の spec ドラフト着手可能性確認 (独立)
- [ ] BL-042 (タスクカードアクション 3 つに削減) の spec ドラフト着手可能性確認 (BL-040 完了後)
- [ ] BL-043 (「現在に設定」ジェスチャ) の spec ドラフト着手可能性確認 (BL-037 + BL-042 完了後)
- [ ] BL-044 (プロジェクト追加ボタン) の spec ドラフト着手可能性確認 (BL-041 完了後)
- [ ] BL-045 (補助 view のシェル統合) の spec ドラフト着手可能性確認 (BL-036 完了後)
- [ ] BL-046 (デザイントークン基盤) の spec ドラフト着手可能性確認 (独立)
- [ ] BL-047 (完了数カウンタ配置) の spec ドラフト着手可能性確認 (U-003 確定後)

## ADR 起票検討 (任意)

- [ ] CSS フレームワーク選定 (vanilla CSS + CSS variables / Tailwind / CSS Modules / CSS-in-JS) の比較を ADR 化するか判断
  - 本 BL の D-006 で vanilla CSS + CSS variables を保守側デフォルトに決定済み. ADR は BL-046 内で起票する選択肢を残す
- [ ] ナビゲーション構造 (左サイドバー固定) の ADR 化を検討
  - モバイルでドロワー化するか (U-001) は後続 BL で別途検討するため, 本 BL では ADR 不要と判断

## Open Questions の処理計画

> spec.md §「未決事項 / 確認待ち」の U-001 〜 U-010 を, どの BL で確定するかをここで割り当てる. ユーザー確認が必要なものは ★ を付ける.

- [ ] **U-001** モバイルでのサイドバー扱い: 別 BL (Capacitor 配下の UI 調整, 本 BL 完了後に backlog 追加検討) ★
- [ ] **U-002** ダークモード対応: 本 BL で「対応しない」を確定 (D-007) ★
- [ ] **U-003** 完了数カウンタの配置: **BL-047** で確定 ★
- [ ] **U-004** 「現在に設定」UX: **BL-043** で確定
- [ ] **U-005** 編集ダイアログのトリガ: **BL-042** または **BL-043** で確定 (アクションシート案を採るなら BL-043 と統合)
- [ ] **U-006** CSS フレームワーク選定: **BL-046** で確定 (本 BL では vanilla CSS + CSS variables を保守側デフォルト)
- [ ] **U-007** プロジェクトトグル UI のオーバーフロー挙動: **BL-041** で確定
- [ ] **U-008** ルーティン由来タスクの「明日にする」非表示/disabled: **BL-042** で確定 (現状維持 = 非表示)
- [ ] **U-009** サイドバーのアクティブ表示: **BL-036** で確定
- [ ] **U-010** 補助 view (settings/trash/routines/projects) のレイアウト刷新範囲: **BL-045** で確定

## ドキュメント

- [ ] 本 BL の spec / plan / tasks が確定したら `docs/developer/planning/backlog.md` の BL-035 のメモ欄に「spec / plan 確定. 後続 BL-036 〜 BL-047 着手可能」を追記
- [ ] 本 BL に関連する ADR が起票されたらリンクを spec / plan に追加 (現時点では起票予定なし)
- [ ] 後続 BL-036 〜 BL-047 の各メモ欄に「依存: BL-035 + ...」の情報を反映するかは, 各後続 BL の spec 起票時に判断

## 仕上げ

- [ ] spec.md / plan.md の状態を「確定」に更新
- [ ] PR 作成 (本 BL は文書のみ. 変更ファイルは `docs/developer/features/ui-redesign-foundation/` 配下 3 ファイルのみのはず)
- [ ] auditor 承認をもって本 BL の Done 判定
