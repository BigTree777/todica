# 設計・実装計画: パスワードリセット

> 記入例。[`spec.md`](spec.md) の要件を実現方法に落とす。

## 方針概要

<例: 期限付きトークンを発行し、メール内リンクから再設定画面に誘導する>

## 影響範囲

| 領域 | 変更内容 |
| --- | --- |
| API | <例: POST /password-reset, POST /password-reset/confirm を追加> |
| DB | <例: password_reset_tokens テーブルを追加> |
| モジュール | <例: auth, mailer> |
| UI | <例: リセット要求 / 新パスワード入力画面> |

## 設計詳細

- データモデル: <例: token(hash), user_id, expires_at>
- 処理フロー: <例: 要求→トークン保存→メール送信→検証→更新→トークン無効化>
- 例外 / エラー処理: <例: 期限切れ・使用済みトークンは 400>

## 重要な決定

- <例: トークンはハッシュ化して保存する → 必要なら ADR 化>

## リスク / 代替案

- <例: メール到達性。代替として有効期限を延ばす案>

## テスト方針

- <例: 期限切れ・再利用・多重要求の異常系を重点確認>
