# 仕様: パスワードリセット

> このディレクトリ（`password-reset/`）は記入例。実際には `_template/` をコピーし、
> ディレクトリ名を英語 kebab-case の機能名に置き換えて使う。

- 状態: ドラフト
- 関連: [`../../planning/backlog.md`](../../planning/backlog.md) の <BL-XXX>

## 背景 / 課題

<例: ユーザーがパスワードを忘れた場合に自力で再設定できず、問い合わせが発生している>

## ゴール / 非ゴール

- ゴール: <例: メール経由でパスワードを再設定できる>
- 非ゴール: <例: SMS によるリセットは対象外>

## 要件

- 機能要件:
  - <例: 登録済みメール宛にリセットリンクを送る>
  - <例: リンクの有効期限は 30 分>
- 非機能要件:
  - <例: トークンは推測困難なランダム値>

## 受け入れ基準

```
シナリオ: パスワードを再設定できる
  Given 登録済みユーザーがリセットを要求した
  When  有効なリンクから新しいパスワードを設定する
  Then  新しいパスワードでログインできる
```

## 未決事項 / 確認待ち

- <例: 送信メールの文面の最終確認>
