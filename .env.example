# 環境変数のサンプル。これをコピーして .env を作成する（.env は Git 管理対象外）。
# 詳細は docs/developer/setup/server.md を参照。実値は決してコミットしないこと。

# Bearer 認証トークン（空だと認証なしで動作する）。
AUTH_TOKEN=

# リッスンポート（既定: 3000）。
PORT=3000

# SQLite データベースファイルのパス（既定: ./todica.db）。
DATABASE_PATH=./todica.db
